# 基于大模型的电力智能运维 Multi-Agent 系统说明文档

## 1. 项目概述

### 1.1 项目名称

**基于大模型的电力智能运维 Multi-Agent 系统**

### 1.2 项目周期

**2025.09 - 2025.12**

### 1.3 技术架构

```text
SpringBoot + SpringAI-Alibaba + RAG + ReAct + Plan-Execute-Replan + Multi-Agent + MCP + SSE
```

### 1.4 项目简介

本项目面向电力巡检、监控告警处理、设备故障排障和现场知识查询场景，设计并实现了一套基于大模型的电力智能运维 Multi-Agent 系统。

系统提供两类核心入口：

1. **对话式智能问答入口**  
   运维人员可以通过自然语言提问，查询电力安规、设备状态、巡检标准、异常日志、历史工单和故障处理流程。

2. **告警驱动自动诊断入口**  
   监控平台、SCADA 系统或设备在线监测系统产生结构化告警事件后，系统自动触发 Agent 诊断流程，完成告警解析、知识检索、排查计划生成、工具调用、日志分析、历史工单分析和运维建议生成。

系统通过 Multi-Agent 架构拆分不同能力模块，包括 Router Agent、Chat Agent、Knowledge Agent、Alarm Agent、Planner Agent、ReAct Agent、Tool Agent、Log Analysis Agent、Ticket Agent 和 Diagnosis Agent。各 Agent 通过编排协同，完成从“用户提问 / 告警触发”到“智能诊断建议输出”的完整闭环。

---

## 2. 项目背景

随着电网规模不断扩大，变电站、配电线路、主变压器、开关柜、断路器、隔离开关、母线、电缆等设备数量持续增加，电力运维工作面临更高的巡检、排障和安全管理压力。

传统电力运维系统通常可以完成数据采集、监控展示和告警推送，但在实际运维过程中仍然存在明显问题。

### 2.1 告警信息只能被动展示，缺少智能分析能力

监控平台、SCADA 系统和设备在线监测系统可以生成告警，但大多数情况下只能告诉运维人员“发生了什么”，无法进一步分析“为什么发生”“应该怎么查”“是否需要派单”。

例如，系统可以提示：

```text
1号主变油温异常升高
```

但传统系统通常不会自动分析：

```text
是否与负荷过高有关？
是否与冷却器异常有关？
是否存在历史重复缺陷？
下一步应该优先检查哪里？
是否需要降负荷或派单处理？
```

### 2.2 运维知识分散，现场查询成本高

电力运维涉及大量知识资料，包括电力安全工作规程、变电运维规程、配网抢修手册、设备厂家说明书、标准化巡检作业指导书、故障处理流程、历史故障案例和专家经验文档等。

这些资料通常分散在不同文档、系统或人员经验中。现场运维人员在处理故障时，需要人工翻阅大量资料，效率较低。

### 2.3 跨系统查询复杂

处理一个设备异常时，运维人员往往需要查询多个系统：

```text
监控告警系统：查看当前告警和历史告警
设备状态系统：查看油温、电流、电压、负荷等指标
日志系统：查看设备运行日志和异常日志
工单系统：查看历史缺陷和检修记录
设备台账系统：查看设备型号、厂家和投运时间
安规系统：查看对应作业安全要求
```

跨系统查询成本高，且依赖人员经验。

### 2.4 故障排查依赖经验

复杂故障通常不是单一原因造成的。比如主变油温异常可能由以下因素导致：

```text
负荷过高
环境温度较高
冷却器风机故障
油泵异常
温控器动作异常
油位异常
传感器误报
控制回路故障
```

新人运维人员很难快速形成完整的排查思路，排障质量和效率容易受到经验水平影响。

### 2.5 缺少统一的智能问答入口

传统运维系统多为菜单式、表格式、报表式操作，现场人员需要知道在哪个系统、哪个页面、哪个字段中查找信息。

而实际业务中，现场人员更希望直接通过自然语言提问：

```text
1号主变最近有没有异常？
主变油温异常应该怎么排查？
进入高压室前需要注意哪些安全事项？
帮我查一下这个设备近半年有没有缺陷。
这个告警需要马上派单吗？
```

因此，需要建设一个统一的智能问答助手，让运维人员可以通过自然语言完成知识查询、设备查询、日志分析、工单查询和故障诊断。

---

## 3. 业务需求

### 3.1 智能问答需求

系统需要提供类似智能助手的自然语言对话能力，支持运维人员主动提问。

#### 典型问题

```text
进入高压室前需要做哪些安全措施？
10kV 开关柜局放异常应该怎么处理？
1号主变现在油温是多少？
这个设备最近有没有异常日志？
帮我查一下1号主变近半年有没有缺陷工单。
主变油温异常可能是什么原因？
这个告警需要马上处理吗？
```

#### 业务目标

1. 让运维人员通过自然语言完成业务查询。
2. 支持安规问答、巡检指导、设备状态查询、日志分析和工单查询。
3. 支持围绕同一设备或同一告警进行多轮追问。
4. 降低现场人员查询系统和资料的成本。

### 3.2 告警自动诊断需求

系统需要能够接收监控平台推送的结构化告警事件，并自动触发诊断流程。

#### 告警输入示例

```json
{
  "alarmId": "ALM202510180032",
  "alarmTime": "2025-10-18 14:32:08",
  "station": "XX变电站",
  "deviceId": "TR-110KV-001",
  "deviceName": "1号主变",
  "deviceType": "变压器",
  "alarmType": "油温异常升高",
  "alarmLevel": "重要",
  "currentValue": "86℃",
  "threshold": "80℃",
  "duration": "12分钟",
  "status": "未处理"
}
```

#### 业务目标

1. 自动解析告警字段。
2. 判断告警类型和风险等级。
3. 检索对应规程和历史案例。
4. 规划排查步骤。
5. 调用外部系统查询监控数据、设备日志和历史工单。
6. 综合生成初步诊断结论和处理建议。

### 3.3 知识库问答需求

系统需要构建电力运维知识库，支持 RAG 检索增强问答。

#### 知识库来源

```text
电力安全工作规程
变电运维规程
配网抢修手册
主变设备说明书
开关柜设备说明书
断路器检修手册
标准化巡检作业指导书
故障处理规程
历史故障案例
专家经验文档
```

#### 业务目标

1. 支持规程、手册、案例的语义检索。
2. 支持自然语言问答。
3. 回答应尽量基于检索内容，降低模型幻觉。
4. 支持故障诊断过程中自动检索相关知识。

### 3.4 外部系统工具调用需求

系统需要通过 MCP 将外部业务系统封装成 Agent 可调用工具。

#### 需要接入的外部系统

```text
监控告警系统
设备状态系统
设备日志系统
缺陷工单系统
安规查询系统
设备台账系统
历史案例库
```

#### 业务目标

1. 让 Agent 能够查询真实业务数据。
2. 支持根据用户问题动态选择工具。
3. 支持根据告警诊断计划自动调用工具。
4. 支持工具调用结果参与最终诊断分析。

### 3.5 多轮对话需求

系统需要支持连续对话和上下文记忆。

#### 示例

```text
用户：帮我分析一下1号主变油温异常。
系统：建议查询油温趋势、负荷变化和冷却器状态。

用户：那它最近有没有类似问题？
系统：这里的“它”指的是1号主变，系统继续查询1号主变历史缺陷工单。

用户：那下一步到现场先看哪里？
系统：结合前文油温异常和历史缺陷，建议优先检查冷却器风机、温控器和控制回路。
```

#### 业务目标

1. 支持指代消解。
2. 支持当前设备上下文维护。
3. 支持当前告警上下文维护。
4. 支持多轮追问和持续分析。

### 3.6 诊断报告生成需求

系统需要输出结构化、可执行的诊断建议。

#### 输出内容

```text
告警摘要
初步判断
分析依据
可能原因
排查步骤
处理建议
安全风险提示
是否建议派单
```

#### 业务目标

1. 让运维人员能快速理解问题。
2. 给出可执行的下一步排查方案。
3. 说明建议的依据。
4. 支持派单、复盘和知识沉淀。

---

## 4. 系统总体架构

### 4.1 架构概览

系统采用 **Multi-Agent + RAG + ReAct + Plan-Execute-Replan + MCP + SSE** 的整体架构。

```text
用户 / 监控平台
   │
   ├── 自然语言问题
   └── 结构化告警事件
          │
          ▼
统一接入层
   ├── 对话请求接入
   ├── 告警事件接入
   └── SSE 流式输出
          │
          ▼
多 Agent 编排层
   ├── Router Agent：意图识别与任务路由
   ├── Chat Agent：多轮对话管理
   ├── Knowledge Agent：规程 / 手册 / 案例知识问答
   ├── Alarm Agent：告警解析与风险判断
   ├── Planner Agent：任务规划与步骤拆解
   ├── ReAct Agent：推理与工具调用
   ├── Tool Agent：外部系统调用
   ├── Log Analysis Agent：日志分析
   ├── Ticket Agent：历史工单分析
   └── Diagnosis Agent：综合诊断与建议生成
          │
          ▼
能力支撑层
   ├── 大语言模型
   ├── SpringAI-Alibaba
   ├── RAG 知识库
   ├── 向量数据库
   ├── Prompt 模板
   ├── 上下文记忆
   └── MCP 工具协议
          │
          ▼
外部业务系统
   ├── 监控告警系统
   ├── 设备状态系统
   ├── 设备日志系统
   ├── 缺陷工单系统
   ├── 安规查询系统
   ├── 设备台账系统
   └── 历史案例库
```

### 4.2 分层说明

#### 用户交互层

主要面向现场运维人员、值班人员和后台管理人员，提供统一的智能问答入口。

功能包括：

```text
自然语言问答
设备状态查询
安规查询
巡检指导
故障诊断
告警分析
诊断报告查看
```

#### 告警接入层

负责接收监控平台、SCADA 或设备在线监测系统推送的结构化告警事件。

功能包括：

```text
告警事件接收
字段校验
格式统一
告警任务创建
推送 Agent 诊断流程
```

#### 多 Agent 编排层

负责根据任务类型编排不同 Agent 协同工作。

核心能力包括：

```text
意图识别
任务路由
上下文管理
知识检索
任务规划
工具调用
日志分析
工单分析
综合诊断
结果生成
```

#### 能力支撑层

提供大模型调用、RAG 检索、Prompt 管理、上下文记忆和 MCP 工具协议支持。

#### 外部系统层

提供真实业务数据来源，包括监控、日志、工单、台账和安规等系统。

---

## 5. Multi-Agent 设计

### 5.1 Router Agent：意图识别与任务路由 Agent

#### 作用

Router Agent 是系统入口调度 Agent，负责识别用户输入或告警事件类型，并将任务分发给对应的业务 Agent。

#### 输入类型

```text
自然语言问题
结构化告警事件
用户补充信息
多轮追问内容
```

#### 可识别意图

```text
安规问答
设备状态查询
监控告警分析
设备日志分析
历史工单查询
现场巡检指导
故障诊断
工单检查
普通问答
```

#### 路由示例

```text
用户问题：进入高压室前需要注意什么？
路由结果：Knowledge Agent

用户问题：帮我查一下1号主变现在油温是多少？
路由结果：Tool Agent + Chat Agent

用户问题：1号主变油温异常，帮我分析一下原因。
路由结果：Knowledge Agent + Planner Agent + Tool Agent + Log Analysis Agent + Ticket Agent + Diagnosis Agent

用户问题：这个告警需要马上派单吗？
路由结果：Alarm Agent + Ticket Agent + Diagnosis Agent
```

### 5.2 Chat Agent：多轮对话 Agent

#### 作用

Chat Agent 负责维护对话上下文，使系统支持连续追问和上下文理解。

#### 核心能力

```text
多轮上下文记忆
当前设备上下文维护
当前告警上下文维护
指代消解
用户意图补全
对话状态管理
```

#### 示例

```text
用户：帮我分析一下1号主变油温异常。
系统：已识别1号主变油温异常，建议查询油温趋势、负荷变化和冷却器状态。

用户：那它最近有没有类似问题？
系统：这里的“它”指的是1号主变，系统继续查询该设备历史缺陷工单。

用户：那下一步先看哪里？
系统：结合前文，建议优先检查冷却器风机、油泵、温控器和控制回路。
```

### 5.3 Knowledge Agent：知识问答 Agent

#### 作用

Knowledge Agent 基于 RAG 检索电力运维知识库，负责回答安规、设备手册、巡检标准和故障处理流程相关问题。

#### 知识来源

```text
电力安全工作规程
变电运维规程
配网抢修手册
设备说明书
标准化作业指导书
历史故障案例
专家经验文档
```

#### 典型问题

```text
进入高压室前需要做哪些安全措施？
主变油温异常应该怎么处理？
10kV 开关柜局放异常有哪些排查步骤？
断路器拒动可能有哪些原因？
```

#### 输出内容

```text
规程依据
处理流程
安全注意事项
排查步骤
风险提示
```

### 5.4 Alarm Agent：告警解析 Agent

#### 作用

Alarm Agent 负责处理监控平台推送的结构化告警事件，解析告警字段并判断初步风险。

#### 输入示例

```json
{
  "deviceName": "1号主变",
  "deviceType": "变压器",
  "alarmType": "油温异常升高",
  "alarmLevel": "重要",
  "currentValue": "86℃",
  "threshold": "80℃",
  "duration": "12分钟"
}
```

#### 输出示例

```json
{
  "device": "1号主变",
  "deviceType": "变压器",
  "alarmCategory": "温度异常",
  "riskLevel": "较高",
  "needDiagnosis": true,
  "suggestedNextSteps": [
    "查询油温趋势",
    "查询负荷变化",
    "查询冷却器运行状态",
    "查询历史缺陷工单"
  ]
}
```

### 5.5 Planner Agent：任务规划 Agent

#### 作用

Planner Agent 用于复杂任务拆解，尤其适合故障诊断场景。

#### 工作方式

```text
输入告警信息 / 用户问题
        ↓
结合知识库检索结果
        ↓
生成初始排查计划
        ↓
交给 Tool Agent 执行
        ↓
根据执行结果动态调整计划
```

#### 示例

输入：

```text
1号主变油温异常升高，当前值86℃，阈值80℃，持续12分钟。
```

输出：

```text
步骤1：查询1号主变最近1小时油温变化趋势。
步骤2：查询1号主变最近1小时负荷变化趋势。
步骤3：查询冷却器风机和油泵运行状态。
步骤4：查询设备日志中是否存在冷却器启动失败记录。
步骤5：查询该设备近半年历史缺陷工单。
步骤6：结合规程和查询结果生成诊断建议。
```

### 5.6 ReAct Agent：推理与工具调用 Agent

#### 作用

ReAct Agent 负责实现“边推理边调用工具”的能力。

当用户问题无法仅靠知识库回答时，ReAct Agent 会判断需要调用哪些工具。

#### 示例

用户问题：

```text
帮我看看1号主变现在是否还在异常状态。
```

ReAct Agent 判断：

```text
需要查询当前告警状态。
需要查询当前油温。
需要查询冷却器运行状态。
```

然后通过 Tool Agent 调用对应工具。

### 5.7 Tool Agent：外部系统调用 Agent

#### 作用

Tool Agent 负责通过 MCP 调用外部业务系统。

#### 可调用工具

```text
getDeviceStatus：查询设备实时状态
getAlarmHistory：查询历史告警
getDeviceLogs：查询设备日志
getDefectTickets：查询历史缺陷工单
searchSafetyRules：查询电力安规
getDeviceProfile：查询设备台账
```

#### 工具调用示例

```json
{
  "toolName": "getDeviceStatus",
  "params": {
    "deviceId": "TR-110KV-001",
    "metrics": ["oilTemperature", "loadRate", "coolerStatus"],
    "timeRange": "1h"
  }
}
```

#### 返回示例

```json
{
  "oilTemperature": "86℃",
  "loadRate": "92%",
  "coolerStatus": "异常"
}
```

### 5.8 Log Analysis Agent：日志分析 Agent

#### 作用

Log Analysis Agent 负责分析设备日志，识别异常链路和故障关联。

#### 输入示例

```text
2025-10-18 14:20:03 冷却器#2 风机启动失败
2025-10-18 14:25:48 温控器切换失败
2025-10-18 14:31:02 主变油温超过告警阈值
```

#### 输出示例

```text
日志显示冷却器#2风机在油温告警前出现启动失败，随后温控器切换失败，最终油温超过阈值。
初步推测本次油温异常与冷却系统故障存在较强关联。
```

### 5.9 Ticket Agent：历史工单分析 Agent

#### 作用

Ticket Agent 负责查询和分析历史缺陷工单、检修记录和相似故障案例。

#### 典型问题

```text
这个设备以前有没有类似故障？
这个缺陷是不是重复出现？
这个工单描述是否完整？
这个告警是否应该派单？
```

#### 输出示例

```text
该设备近两个月出现过2次冷却器相关缺陷，其中一次为温控器动作不灵敏，一次为冷却器控制回路异常。
本次告警与历史缺陷具有相似性，建议按重复缺陷处理，并重点检查控制回路。
```

### 5.10 Diagnosis Agent：综合诊断 Agent

#### 作用

Diagnosis Agent 负责整合多个 Agent 的输出，形成最终诊断结论和运维建议。

#### 输入来源

```text
告警解析结果
知识库检索结果
设备状态数据
日志分析结果
历史工单结果
用户补充信息
```

#### 输出格式

```text
告警摘要
初步判断
分析依据
可能原因
排查步骤
处理建议
安全风险提示
是否建议派单
```

---

## 6. 核心业务流程

### 6.1 对话式智能问答流程

#### 流程说明

用户通过智能问答助手输入自然语言问题，系统由 Router Agent 识别意图，再调用对应 Agent 完成处理。

```text
用户输入自然语言问题
        ↓
Chat Agent 获取上下文
        ↓
Router Agent 识别问题意图
        ↓
判断任务类型
        ↓
分发给对应 Agent
        ↓
Knowledge Agent / Tool Agent / Log Agent / Ticket Agent / Diagnosis Agent 协同处理
        ↓
生成回答
        ↓
SSE 流式返回给用户
```

#### 示例 1：安规问答

用户输入：

```text
进入高压室前需要做哪些安全措施？
```

处理流程：

```text
Router Agent 判断为安规问答
        ↓
Knowledge Agent 检索电力安全工作规程
        ↓
生成安全措施说明
        ↓
返回用户
```

#### 示例 2：设备状态查询

用户输入：

```text
帮我查一下1号主变现在油温是多少？
```

处理流程：

```text
Router Agent 判断为设备状态查询
        ↓
Tool Agent 调用设备状态查询工具
        ↓
获取实时油温数据
        ↓
Chat Agent 组织回答
        ↓
返回用户
```

#### 示例 3：复杂故障问答

用户输入：

```text
1号主变油温异常，帮我分析一下原因。
```

处理流程：

```text
Router Agent 判断为故障诊断
        ↓
Alarm Agent 解析异常类型
        ↓
Knowledge Agent 检索主变油温异常规程
        ↓
Planner Agent 生成排查计划
        ↓
Tool Agent 查询监控数据和日志
        ↓
Ticket Agent 查询历史工单
        ↓
Diagnosis Agent 综合分析
        ↓
生成诊断建议
```

### 6.2 告警驱动自动诊断流程

#### 流程说明

当监控平台产生告警后，系统自动触发 Agent 诊断流程。

```text
现场设备/传感器产生运行数据
        ↓
监控平台判断异常
        ↓
生成结构化告警事件
        ↓
推送给智能运维 Agent 系统
        ↓
Alarm Agent 解析告警
        ↓
Planner Agent 生成排查计划
        ↓
Knowledge Agent 检索规程和案例
        ↓
Tool Agent 查询设备状态、日志和工单
        ↓
Diagnosis Agent 综合诊断
        ↓
生成处理建议
        ↓
必要时建议派单
```

### 6.3 Plan-Execute-Replan 复杂诊断流程

```text
接收异常告警或故障诊断问题
        ↓
Plan：生成初始排查计划
        ↓
Execute：逐步执行工具调用
        ↓
Observe：观察工具返回结果
        ↓
Replan：根据中间结果调整排查方向
        ↓
Final：生成最终诊断建议
```

#### 示例

初始计划：

```text
1. 查询油温趋势。
2. 查询负荷趋势。
3. 查询冷却器状态。
4. 查询设备日志。
5. 查询历史工单。
6. 检索处理规程。
```

工具返回结果：

```text
冷却器#2风机启动失败。
```

系统重新规划：

```text
重点检查冷却器#2风机、接触器、温控器和控制回路。
```

---

## 7. 核心技术方案

### 7.1 RAG 知识库方案

#### 设计目标

RAG 用于解决大模型不了解企业内部电力知识、容易产生幻觉的问题。系统在回答安规、设备手册、巡检标准和故障处理问题时，会先从知识库中检索相关内容，再结合检索结果生成回答。

#### 文档处理流程

```text
原始文档上传
    ↓
文档解析
    ↓
文本清洗
    ↓
分段切片
    ↓
Embedding 向量化
    ↓
写入向量数据库
    ↓
建立索引
```

#### 检索流程

```text
用户问题 / 告警信息
    ↓
生成检索 Query
    ↓
向量检索相关片段
    ↓
关键词检索补充
    ↓
结果重排序
    ↓
组装 Prompt
    ↓
大模型生成回答
```

#### 适用场景

```text
安规问答
设备手册查询
巡检标准查询
故障处理流程查询
历史案例检索
诊断建议依据补充
```

### 7.2 ReAct 工具调用方案

#### 设计目标

ReAct 用于让大模型具备“推理 + 工具调用”的能力。在电力运维场景中，很多问题必须查询外部系统才能回答。

例如：

```text
这个告警现在恢复了吗？
1号主变现在油温是多少？
最近是否有冷却器启动失败日志？
这个设备近半年有没有类似缺陷？
```

#### 工作流程

```text
用户问题 / 告警事件
    ↓
模型分析问题
    ↓
判断是否需要调用工具
    ↓
选择合适工具
    ↓
调用 MCP 工具
    ↓
获取工具返回结果
    ↓
继续推理分析
    ↓
生成最终回答
```

### 7.3 Plan-Execute-Replan 方案

#### 设计目标

Plan-Execute-Replan 用于复杂故障诊断任务。电力故障诊断通常不是一次查询就能完成，需要分步骤执行，并根据中间结果调整后续排查方向。

#### 工作流程

```text
Plan：生成初始排查计划
Execute：执行计划中的步骤
Tool Call：调用外部系统工具
Observe：观察工具返回结果
Replan：根据中间结果重新规划
Final：生成最终诊断建议
```

#### 业务价值

```text
支持复杂故障拆解
支持动态调整排查方向
减少一次性生成错误建议的风险
提升诊断过程可解释性
更贴近人工专家排障思路
```

### 7.4 MCP 工具接入方案

#### 设计目标

MCP 用于将外部系统封装成大模型可调用工具，使 Agent 能访问真实业务数据。

#### 工具列表

| 工具名称 | 功能说明 |
|---|---|
| getDeviceStatus | 查询设备实时运行状态 |
| getAlarmHistory | 查询设备历史告警 |
| getDeviceLogs | 查询设备运行日志 |
| getDefectTickets | 查询历史缺陷工单 |
| searchSafetyRules | 查询电力安规 |
| getDeviceProfile | 查询设备台账 |

#### 工具调用示例

```json
{
  "toolName": "getDeviceLogs",
  "params": {
    "deviceId": "TR-110KV-001",
    "timeRange": "1h",
    "keywords": ["冷却器", "启动失败", "温控器"]
  }
}
```

### 7.5 SSE 流式输出方案

#### 设计目标

复杂诊断和长文本回答生成时间较长，系统通过 SSE 实现流式输出，提升用户体验。

#### 适用场景

```text
智能问答长答案
故障诊断报告
巡检指导生成
安规说明生成
故障复盘报告
```

#### 输出示例

```text
正在解析告警信息...
已识别设备：1号主变。
正在检索主变油温异常处理规程...
正在查询最近1小时油温趋势...
正在分析冷却器日志...
正在查询历史工单...
正在生成诊断建议...
```

---

---

## 8. RAG 文档上传与知识库构建服务设计

前面的方案中已经说明了 RAG 的检索流程，但在真实系统中，还需要单独设计一个 **文档上传与知识库构建服务**。该服务负责把电力规程、设备手册、巡检标准、历史案例等原始文档处理成可被大模型检索的知识库数据。

### 8.1 服务定位

RAG 文档上传服务主要面向后台管理员、运维专家或知识库维护人员，用于维护电力运维知识库。

它不是直接面向普通问答用户的功能，而是系统的知识底座建设模块。

系统整体可以分为两条链路：

```text
知识库构建链路：
原始文档上传 → 文档解析 → 文本清洗 → 分段切片 → Embedding 向量化 → 写入向量数据库 → 建立索引

知识库问答链路：
用户问题 / 告警信息 → 检索知识库 → 召回相关片段 → 组装 Prompt → 大模型生成回答
```

其中，文档上传服务负责第一条链路，Knowledge Agent 和 Diagnosis Agent 主要使用第二条链路。

---

### 8.2 业务需求

RAG 文档上传服务需要支持以下能力：

1. **支持多类型文档上传**
   - PDF
   - Word
   - Markdown
   - TXT
   - HTML
   - Excel 表格类知识文档
   - 设备说明书扫描件，后续可扩展 OCR

2. **支持文档分类管理**
   - 电力安规
   - 设备手册
   - 巡检标准
   - 故障处理规程
   - 历史故障案例
   - 专家经验文档
   - 厂家说明书
   - 标准作业指导书

3. **支持文档解析和文本抽取**
   - 提取正文内容
   - 保留标题层级
   - 识别章节结构
   - 提取表格内容
   - 过滤页眉、页脚、目录、页码等无效文本

4. **支持文档切片**
   - 按章节切片
   - 按段落切片
   - 按 token 长度切片
   - 保留上下文重叠窗口
   - 保存文档来源、章节标题、页码等元数据

5. **支持向量化与索引构建**
   - 调用 Embedding 模型生成文本向量
   - 写入向量数据库
   - 建立文档级、章节级、片段级索引
   - 支持后续语义检索和混合检索

6. **支持文档版本管理**
   - 文档更新后重新解析和向量化
   - 支持旧版本归档
   - 支持启用 / 禁用某个文档
   - 支持删除文档时同步删除向量数据

7. **支持知识库检索验证**
   - 上传完成后可以输入测试问题
   - 查看召回片段是否准确
   - 便于管理员调整切片策略和文档质量

---

### 8.3 文档上传处理流程

RAG 文档上传服务的核心处理流程如下：

```text
管理员上传原始文档
        ↓
文件合法性校验
        ↓
保存原始文件
        ↓
创建文档处理任务
        ↓
文档解析
        ↓
文本清洗
        ↓
分段切片
        ↓
生成 Embedding 向量
        ↓
写入向量数据库
        ↓
写入文档元数据
        ↓
建立检索索引
        ↓
文档状态更新为可检索
```

对应到项目中的核心链路可以描述为：

```text
原始文档上传
    ↓
文档解析
    ↓
文本清洗
    ↓
分段切片
    ↓
Embedding 向量化
    ↓
写入向量数据库
    ↓
建立索引
```

---

### 8.4 文档上传服务模块设计

#### 8.4.1 文档上传模块

负责接收用户上传的原始文档，并进行基础校验。

校验内容包括：

```text
文件类型是否合法
文件大小是否超限
文件是否重复上传
文档分类是否填写
文档来源是否填写
文档名称是否为空
```

上传后，系统会保存原始文件，并在数据库中创建一条文档记录。

#### 8.4.2 文档解析模块

负责将不同格式的文件解析成纯文本或结构化文本。

不同文档类型的解析方式不同：

| 文档类型 | 解析方式 |
|---|---|
| PDF | 提取页面文本、表格和章节 |
| Word | 提取标题、正文、表格 |
| Markdown | 直接解析标题层级和正文 |
| TXT | 按段落解析 |
| HTML | 提取正文并清理标签 |
| Excel | 按 sheet、行列解析为结构化文本 |

解析后的文本会尽量保留原始文档的结构，例如标题、章节、页码和表格说明。

#### 8.4.3 文本清洗模块

负责清理影响检索质量的无效内容。

常见清洗规则包括：

```text
删除页眉、页脚、页码
删除重复空行
删除目录中的无效跳转内容
统一全角 / 半角字符
统一特殊符号
过滤过短或无意义段落
合并断行文本
保留标题层级
```

例如，设备手册中经常会出现大量页眉、页脚和目录页，如果不清洗，会影响向量检索结果。

#### 8.4.4 分段切片模块

负责把长文档拆分成适合向量检索的文本块。

切片策略可以根据文档类型选择：

```text
安规类文档：优先按条款和章节切片
设备手册：优先按章节和功能模块切片
故障案例：优先按案例单元切片
巡检标准：优先按检查项目切片
普通文档：按 token 长度切片
```

切片时需要保留一定上下文重叠，避免关键信息被切断。

示例：

```text
chunkSize：800 tokens
chunkOverlap：100 tokens
```

每个切片需要保存元数据：

```json
{
  "documentId": "DOC202510180001",
  "documentName": "主变压器运行维护手册.pdf",
  "documentType": "设备手册",
  "chapter": "第三章 冷却系统运行维护",
  "pageNo": 26,
  "chunkIndex": 12,
  "content": "当主变油温异常升高时，应检查负荷情况、冷却器运行状态、油位和温控装置..."
}
```

#### 8.4.5 Embedding 向量化模块

负责调用 Embedding 模型，将文本切片转换为向量。

处理流程：

```text
读取待向量化文本块
        ↓
调用 Embedding 模型
        ↓
生成向量结果
        ↓
绑定 chunkId 和元数据
        ↓
写入向量数据库
```

向量化后，每个文本块都会对应一个向量，后续用户提问时可以通过语义相似度召回相关内容。

#### 8.4.6 向量数据库写入模块

负责将文本块、向量和元数据写入向量数据库。

存储内容包括：

```text
chunkId
documentId
documentName
documentType
chapter
pageNo
content
embedding
createTime
status
```

后续检索时，系统可以基于向量相似度召回片段，也可以基于 documentType、设备类型、文档来源等元数据进行过滤。

#### 8.4.7 索引构建模块

负责建立检索索引，支持高效召回。

索引类型可以包括：

```text
向量索引
关键词索引
文档分类索引
设备类型索引
章节索引
```

项目中可以采用混合检索方式：

```text
向量检索 + 关键词检索 + 元数据过滤 + 重排序
```

这样可以提升电力专业术语、设备编号、规程条款等场景下的检索准确性。

---

### 8.5 RAG 文档上传接口设计

#### 8.5.1 上传文档接口

##### 请求地址

```http
POST /api/knowledge/documents/upload
```

##### 请求类型

```text
multipart/form-data
```

##### 请求参数

| 参数名 | 类型 | 说明 |
|---|---|---|
| file | File | 上传的原始文档 |
| documentName | String | 文档名称 |
| documentType | String | 文档类型 |
| source | String | 文档来源 |
| description | String | 文档描述 |
| tags | String | 文档标签 |

##### 请求示例

```text
file: 主变压器运行维护手册.pdf
documentName: 主变压器运行维护手册
documentType: 设备手册
source: 厂家资料
tags: 主变,冷却器,油温
```

##### 返回结果

```json
{
  "code": 200,
  "message": "文档上传成功，正在处理中",
  "data": {
    "documentId": "DOC202510180001",
    "taskId": "KTASK202510180001",
    "status": "PROCESSING"
  }
}
```

---

#### 8.5.2 查询文档处理状态接口

##### 请求地址

```http
GET /api/knowledge/documents/{documentId}/status
```

##### 返回结果

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "documentId": "DOC202510180001",
    "documentName": "主变压器运行维护手册",
    "status": "COMPLETED",
    "chunkCount": 128,
    "embeddingCount": 128,
    "errorMessage": null
  }
}
```

---

#### 8.5.3 文档列表查询接口

##### 请求地址

```http
GET /api/knowledge/documents
```

##### 请求参数

| 参数名 | 类型 | 说明 |
|---|---|---|
| documentType | String | 文档类型 |
| keyword | String | 搜索关键词 |
| status | String | 文档状态 |
| page | Integer | 页码 |
| size | Integer | 每页数量 |

##### 返回结果

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 2,
    "records": [
      {
        "documentId": "DOC202510180001",
        "documentName": "主变压器运行维护手册",
        "documentType": "设备手册",
        "status": "COMPLETED",
        "chunkCount": 128,
        "createTime": "2025-10-18 15:00:00"
      }
    ]
  }
}
```

---

#### 8.5.4 删除文档接口

##### 请求地址

```http
DELETE /api/knowledge/documents/{documentId}
```

##### 功能说明

删除文档时，需要同步删除：

```text
原始文件
文档元数据
文本切片
向量数据库中的 embedding 数据
相关索引
```

##### 返回结果

```json
{
  "code": 200,
  "message": "文档删除成功"
}
```

---

#### 8.5.5 知识库检索测试接口

##### 请求地址

```http
POST /api/knowledge/search/test
```

##### 请求参数

```json
{
  "question": "主变油温异常升高应该怎么处理？",
  "documentType": "设备手册",
  "topK": 5
}
```

##### 返回结果

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "question": "主变油温异常升高应该怎么处理？",
    "results": [
      {
        "documentId": "DOC202510180001",
        "documentName": "主变压器运行维护手册",
        "chapter": "第三章 冷却系统运行维护",
        "pageNo": 26,
        "score": 0.87,
        "content": "当主变油温异常升高时，应检查负荷情况、冷却器运行状态、油位和温控装置..."
      }
    ]
  }
}
```

---

### 8.6 RAG 文档相关数据库设计

#### 8.6.1 知识文档表 knowledge_document

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| document_id | varchar | 文档编号 |
| document_name | varchar | 文档名称 |
| document_type | varchar | 文档类型 |
| source | varchar | 文档来源 |
| file_path | varchar | 原始文件路径 |
| file_type | varchar | 文件类型 |
| file_size | bigint | 文件大小 |
| status | varchar | 处理状态 |
| chunk_count | int | 切片数量 |
| embedding_count | int | 向量数量 |
| description | text | 文档描述 |
| tags | varchar | 标签 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

#### 8.6.2 知识切片表 knowledge_chunk

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| chunk_id | varchar | 切片编号 |
| document_id | varchar | 文档编号 |
| chunk_index | int | 切片序号 |
| chapter | varchar | 所属章节 |
| page_no | int | 页码 |
| content | text | 切片内容 |
| token_count | int | token 数量 |
| status | varchar | 状态 |
| create_time | datetime | 创建时间 |

#### 8.6.3 文档处理任务表 knowledge_process_task

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| task_id | varchar | 任务编号 |
| document_id | varchar | 文档编号 |
| task_type | varchar | 任务类型 |
| status | varchar | 任务状态 |
| current_step | varchar | 当前步骤 |
| error_message | text | 错误信息 |
| start_time | datetime | 开始时间 |
| end_time | datetime | 结束时间 |
| create_time | datetime | 创建时间 |

#### 8.6.4 向量数据说明

向量数据通常存储在向量数据库中，核心字段包括：

| 字段名 | 说明 |
|---|---|
| chunk_id | 切片编号 |
| document_id | 文档编号 |
| embedding | 文本向量 |
| content | 切片原文 |
| metadata | 文档名称、类型、章节、页码等元数据 |

---

### 8.7 文档处理状态设计

文档从上传到可检索，需要经历多个状态。

```text
UPLOADED：已上传
PARSING：解析中
CLEANING：清洗中
CHUNKING：切片中
EMBEDDING：向量化中
INDEXING：索引构建中
COMPLETED：处理完成，可检索
FAILED：处理失败
DISABLED：已停用
```

状态流转如下：

```text
UPLOADED
   ↓
PARSING
   ↓
CLEANING
   ↓
CHUNKING
   ↓
EMBEDDING
   ↓
INDEXING
   ↓
COMPLETED
```

如果任一步失败，则进入：

```text
FAILED
```

管理员可以重新触发处理任务。

---

### 8.8 与 Multi-Agent 的关系

RAG 文档上传服务构建出的知识库，会被多个 Agent 使用。

| Agent | 使用知识库的方式 |
|---|---|
| Knowledge Agent | 用于安规问答、手册查询、巡检标准查询 |
| Alarm Agent | 根据告警类型检索相关规程 |
| Planner Agent | 根据规程内容生成排查计划 |
| Diagnosis Agent | 将知识库内容作为诊断依据 |
| Chat Agent | 在多轮问答中补充专业知识 |
| Ticket Agent | 可结合历史案例知识进行相似故障分析 |

例如，当用户问：

```text
主变油温异常应该怎么处理？
```

系统会执行：

```text
用户问题
   ↓
Router Agent 判断为故障处理知识问答
   ↓
Knowledge Agent 检索 RAG 知识库
   ↓
召回《主变压器运行维护手册》中油温异常相关章节
   ↓
大模型结合召回内容生成回答
```

当监控平台推送告警：

```text
1号主变油温异常升高
```

系统会执行：

```text
Alarm Agent 解析告警
   ↓
Knowledge Agent 检索油温异常处理规程
   ↓
Planner Agent 基于规程生成排查计划
   ↓
Tool Agent 查询实时数据
   ↓
Diagnosis Agent 综合生成诊断建议
```

---

### 8.9 简历中可以体现的描述

可以在职责描述中增加一条：

```text
负责 RAG 知识库构建服务开发，支持电力安规、设备手册、巡检标准、故障案例等原始文档上传，完成文档解析、文本清洗、分段切片、Embedding 向量化、向量数据库写入和索引构建，为 Knowledge Agent、Planner Agent 和 Diagnosis Agent 提供知识检索能力。
```

也可以写得更简洁一些：

```text
设计并实现 RAG 文档上传与知识库构建链路，支持原始文档上传、解析清洗、分段切片、向量化存储和索引构建，为电力安规问答、巡检指导和故障诊断提供知识增强能力。
```

## 9. 接口设计示例

### 8.1 对话问答接口

#### 请求地址

```http
POST /api/chat
```

#### 请求参数

```json
{
  "sessionId": "SESSION202510180001",
  "userId": "U10001",
  "message": "帮我分析一下1号主变油温异常的原因。"
}
```

#### 返回结果

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "answer": "初步判断该异常可能与冷却器运行异常有关，建议查询油温趋势、负荷变化和冷却器日志。",
    "intent": "FAULT_DIAGNOSIS"
  }
}
```

### 8.2 SSE 流式问答接口

#### 请求地址

```http
GET /api/chat/stream?sessionId=SESSION202510180001
```

#### 返回事件示例

```text
event: message
data: 正在识别问题意图...

event: message
data: 已识别为故障诊断类问题。

event: message
data: 正在查询设备状态...

event: message
data: 正在生成诊断建议...
```

### 8.3 告警接入接口

#### 请求地址

```http
POST /api/alarm/receive
```

#### 请求参数

```json
{
  "alarmId": "ALM202510180032",
  "alarmTime": "2025-10-18 14:32:08",
  "station": "XX变电站",
  "deviceId": "TR-110KV-001",
  "deviceName": "1号主变",
  "deviceType": "变压器",
  "alarmType": "油温异常升高",
  "alarmLevel": "重要",
  "currentValue": "86℃",
  "threshold": "80℃",
  "duration": "12分钟"
}
```

#### 返回结果

```json
{
  "code": 200,
  "message": "告警接收成功",
  "data": {
    "taskId": "TASK202510180001",
    "status": "PROCESSING"
  }
}
```

### 8.4 智能诊断接口

#### 请求地址

```http
POST /api/agent/diagnose
```

#### 请求参数

```json
{
  "taskId": "TASK202510180001",
  "alarmId": "ALM202510180032"
}
```

#### 返回结果

```json
{
  "code": 200,
  "message": "诊断完成",
  "data": {
    "summary": "1号主变油温异常升高，可能与冷却器运行异常有关。",
    "reasons": [
      "当前油温超过阈值",
      "负荷率较高",
      "冷却器存在启动失败日志",
      "历史工单存在类似缺陷"
    ],
    "suggestions": [
      "检查冷却器#2风机电源和接触器",
      "检查温控器动作情况",
      "持续观察油温变化",
      "必要时采取降负荷措施"
    ]
  }
}
```

---

## 10. 数据库设计示例

### 9.1 会话表 chat_session

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| session_id | varchar | 会话编号 |
| user_id | varchar | 用户编号 |
| current_device_id | varchar | 当前上下文设备编号 |
| current_alarm_id | varchar | 当前上下文告警编号 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

### 9.2 消息表 chat_message

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| session_id | varchar | 会话编号 |
| role | varchar | user / assistant / system |
| content | text | 消息内容 |
| intent | varchar | 识别意图 |
| create_time | datetime | 创建时间 |

### 9.3 告警任务表 alarm_task

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| task_id | varchar | 任务编号 |
| alarm_id | varchar | 告警编号 |
| device_id | varchar | 设备编号 |
| device_name | varchar | 设备名称 |
| alarm_type | varchar | 告警类型 |
| alarm_level | varchar | 告警等级 |
| status | varchar | 任务状态 |
| diagnosis_result | text | 诊断结果 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

### 9.4 Agent 执行记录表 agent_execution_log

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| task_id | varchar | 任务编号 |
| session_id | varchar | 会话编号 |
| agent_name | varchar | Agent 名称 |
| step_name | varchar | 执行步骤 |
| input | text | 输入内容 |
| output | text | 输出内容 |
| status | varchar | 执行状态 |
| create_time | datetime | 创建时间 |

### 9.5 工具调用记录表 tool_call_log

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| task_id | varchar | 任务编号 |
| session_id | varchar | 会话编号 |
| tool_name | varchar | 工具名称 |
| request_param | text | 请求参数 |
| response_data | text | 返回数据 |
| status | varchar | 调用状态 |
| cost_time | bigint | 耗时 |
| create_time | datetime | 创建时间 |

### 9.6 知识文档表 knowledge_document

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| document_name | varchar | 文档名称 |
| document_type | varchar | 文档类型 |
| source | varchar | 来源 |
| status | varchar | 处理状态 |
| create_time | datetime | 创建时间 |

---

## 11. Prompt 设计示例

### 10.1 Router Agent Prompt

```text
你是电力智能运维系统的意图识别与任务路由 Agent。
请根据用户输入判断任务类型，并输出需要调用的 Agent 列表。

可选任务类型：
1. 安规问答
2. 设备状态查询
3. 告警分析
4. 日志分析
5. 历史工单查询
6. 巡检指导
7. 故障诊断
8. 工单检查
9. 普通问答

用户输入：
{user_input}

请输出 JSON：
{
  "intent": "",
  "agents": [],
  "needTool": true/false,
  "needRag": true/false
}
```

### 10.2 Knowledge Agent Prompt

```text
你是电力运维知识问答专家。
请根据用户问题和知识库检索结果，回答用户问题。

要求：
1. 回答必须结合知识库内容。
2. 如果知识库内容不足，请说明信息不足。
3. 涉及安全操作时，必须提示遵守现场规程。
4. 输出应清晰、可执行。

用户问题：
{question}

知识库内容：
{retrieved_docs}
```

### 10.3 Alarm Agent Prompt

```text
你是电力运维告警解析专家。
请根据输入的结构化告警事件，提取关键信息并判断风险等级。

需要提取：
1. 设备名称
2. 设备类型
3. 告警类型
4. 告警等级
5. 当前值和阈值
6. 持续时间
7. 初步风险判断
8. 建议后续查询的数据

告警信息：
{alarm_json}
```

### 10.4 Planner Agent Prompt

```text
你是电力设备故障排查规划专家。
请根据告警信息、用户问题和知识库内容，生成排查计划。

要求：
1. 按步骤输出。
2. 每一步说明需要查询的数据或调用的工具。
3. 优先考虑安全规程。
4. 如果信息不足，请说明需要补充哪些数据。
5. 不要直接下最终结论，先生成排查计划。

告警信息：
{alarm_info}

用户问题：
{question}

知识库内容：
{retrieved_docs}
```

### 10.5 Diagnosis Agent Prompt

```text
你是电力智能运维诊断专家。
请结合告警信息、知识库内容、监控数据、日志信息和历史工单，生成诊断建议。

输出格式：
1. 告警摘要
2. 初步判断
3. 分析依据
4. 可能原因
5. 排查步骤
6. 处理建议
7. 安全风险提示
8. 是否建议派单

告警信息：
{alarm_info}

知识库内容：
{docs}

监控数据：
{metrics}

日志信息：
{logs}

历史工单：
{tickets}
```

---

## 12. 典型业务案例

### 11.1 案例一：用户主动问答诊断

#### 用户输入

```text
帮我分析一下1号主变油温异常的原因。
```

#### 系统执行流程

```text
1. Chat Agent 获取当前对话上下文。
2. Router Agent 识别为故障诊断类问题。
3. Knowledge Agent 检索主变油温异常处理规程。
4. Planner Agent 生成排查计划。
5. Tool Agent 查询油温趋势、负荷趋势、冷却器状态。
6. Log Analysis Agent 分析设备日志。
7. Ticket Agent 查询历史缺陷工单。
8. Diagnosis Agent 综合生成结论。
```

#### 输出结果

```text
初步判断：1号主变油温异常可能与冷却器运行异常有关。

分析依据：
1. 当前油温超过阈值并持续上升。
2. 负荷率较高，散热压力增大。
3. 日志显示冷却器#2风机启动失败。
4. 历史工单显示该设备曾出现冷却器控制回路异常。

建议：
1. 优先检查冷却器#2风机电源和接触器。
2. 检查温控器动作是否正常。
3. 持续观察油温变化。
4. 如油温继续升高，建议按规程采取降负荷措施。
```

### 11.2 案例二：告警自动诊断

#### 告警输入

```json
{
  "alarmId": "ALM202510180032",
  "deviceName": "1号主变",
  "deviceType": "变压器",
  "alarmType": "油温异常升高",
  "alarmLevel": "重要",
  "currentValue": "86℃",
  "threshold": "80℃",
  "duration": "12分钟"
}
```

#### 系统执行流程

```text
1. Alarm Agent 解析告警字段。
2. Planner Agent 生成诊断计划。
3. Knowledge Agent 检索规程。
4. Tool Agent 查询设备状态。
5. Log Analysis Agent 分析冷却器日志。
6. Ticket Agent 查询历史工单。
7. Diagnosis Agent 生成诊断建议。
```

#### 输出结果

```text
告警摘要：
1号主变发生油温异常升高告警，当前油温86℃，超过阈值80℃，持续12分钟，告警等级为重要。

初步判断：
该告警可能与冷却器运行异常有关。

分析依据：
1. 油温超过阈值并持续升高。
2. 负荷率维持在较高水平。
3. 冷却器#2风机存在启动失败记录。
4. 历史工单显示该设备曾出现冷却器控制回路异常。

处理建议：
1. 优先检查冷却器#2风机、接触器和控制回路。
2. 检查温控器动作逻辑是否正常。
3. 持续监测油温变化。
4. 如油温继续升高，应按规程采取降负荷措施。
5. 建议生成重要缺陷工单并派发检修班组处理。
```

### 11.3 案例三：安规问答

#### 用户输入

```text
进入高压室前需要注意哪些安全事项？
```

#### 系统执行流程

```text
1. Router Agent 判断为安规问答。
2. Knowledge Agent 检索电力安全工作规程。
3. 生成作业安全提示。
```

#### 输出结果

```text
进入高压室前应确认工作票和许可手续，核对设备名称和编号，确认现场安全措施已布置到位。
进入前应检查安全工器具状态，按要求佩戴安全帽、绝缘鞋等防护用品。
严禁擅自移动遮栏、标示牌或越过安全围栏。
涉及带电设备附近作业时，应保持规定安全距离，并服从现场负责人统一指挥。
```

### 11.4 案例四：历史工单查询

#### 用户输入

```text
帮我查一下1号主变近半年有没有类似油温异常的缺陷。
```

#### 系统执行流程

```text
1. Router Agent 判断为历史工单查询。
2. Tool Agent 调用工单查询工具。
3. Ticket Agent 分析相似缺陷。
4. Chat Agent 生成回答。
```

#### 输出结果

```text
查询到1号主变近半年存在2条与油温或冷却系统相关的缺陷记录：

1. 2025-09-03，1号主变油温偏高，原因为温控器动作不灵敏，状态为已处理。
2. 2025-10-02，冷却器控制回路间歇性异常，状态为待复查。

本次油温异常与历史缺陷存在一定相似性，建议重点检查温控器和冷却器控制回路。
```

---

## 13. 项目难点与解决方案

### 12.1 电力业务知识专业性强

#### 难点

电力运维涉及大量专业术语、设备结构、安规要求和故障处理流程，普通大模型容易回答不准确。

#### 解决方案

```text
通过 RAG 接入权威规程、设备手册和历史案例。
关键问题先检索知识库，再结合检索结果生成回答。
涉及安全操作时增加安全提示和规程约束。
```

### 12.2 多系统数据分散

#### 难点

监控数据、日志数据、工单数据和设备台账分散在不同系统中，无法直接统一分析。

#### 解决方案

```text
通过 MCP 将外部系统封装为统一工具。
Tool Agent 根据任务动态调用不同工具。
工具返回结果统一结构化后交给 Diagnosis Agent 分析。
```

### 12.3 故障诊断流程复杂

#### 难点

复杂故障需要多步骤查询和动态判断，不适合一次性生成答案。

#### 解决方案

```text
使用 Plan-Execute-Replan 模式。
先生成排查计划，再逐步执行工具调用。
根据中间结果动态调整排查方向。
```

### 12.4 多轮对话上下文管理复杂

#### 难点

用户可能连续追问，并使用“它”“这个告警”“刚才那个设备”等指代词。

#### 解决方案

```text
Chat Agent 维护当前设备、当前告警和当前诊断任务上下文。
对用户追问进行指代消解。
将上下文传递给 Router Agent 和业务 Agent。
```

### 12.5 长文本响应等待时间长

#### 难点

诊断报告、排查步骤和安规说明通常较长，一次性返回体验较差。

#### 解决方案

```text
使用 SSE 实现流式输出。
实时返回分析进度、工具调用状态和生成内容。
提升现场人员交互体验。
```

---

## 14. 项目亮点

1. **智能问答 + 告警诊断双入口**  
   系统既支持运维人员主动自然语言提问，也支持监控平台告警自动触发诊断。

2. **Multi-Agent 架构清晰**  
   通过 Router Agent、Chat Agent、Knowledge Agent、Alarm Agent、Planner Agent、ReAct Agent、Tool Agent、Log Analysis Agent、Ticket Agent 和 Diagnosis Agent 分工协作，提升系统可维护性和扩展性。

3. **业务场景真实**  
   覆盖电力安规问答、现场巡检指导、设备状态查询、日志分析、历史工单查询、告警诊断和故障排查。

4. **RAG 提升专业准确性**  
   通过电力规程、设备手册和历史案例知识库增强回答质量，降低模型幻觉。

5. **MCP 打通外部系统**  
   将监控、日志、工单、台账、安规查询等系统封装为工具，使 Agent 能调用真实业务数据。

6. **Plan-Execute-Replan 支持复杂诊断**  
   对复杂故障进行步骤规划、执行和动态重规划，适合电力故障排查场景。

7. **ReAct 支持动态工具调用**  
   根据用户问题和中间推理结果自动选择工具，提高智能化程度。

8. **SSE 提升交互体验**  
   对长文本回答和复杂诊断过程进行流式输出，减少等待时间。

9. **运维效率提升明显**  
   将原本依赖人工翻规程、查日志、查工单的小时级初步排障流程压缩到分钟级。

---

## 15. 可扩展方向

### 14.1 多模态巡检

后续可以接入图片、视频、红外图谱和声音数据，实现：

```text
设备外观缺陷识别
红外测温异常识别
局放图谱分析
安全帽佩戴检测
现场异常声音识别
```

### 14.2 自动派单闭环

将 Agent 生成的诊断建议与工单系统进一步打通，实现：

```text
自动生成缺陷工单
自动推荐缺陷等级
自动匹配抢修班组
自动推荐备品备件
自动跟踪处理进度
自动生成故障复盘报告
```

### 14.3 电力知识图谱

在 RAG 基础上构建设备故障知识图谱，建立：

```text
设备 - 部件关系
故障现象 - 故障原因关系
故障原因 - 处理措施关系
安规条款 - 作业场景关系
历史缺陷 - 设备型号关系
```

提升复杂因果推理能力。

### 14.4 移动端现场助手

开发移动端或小程序，支持：

```text
扫码识别设备
语音提问
拍照上传异常
自动生成巡检记录
查询历史缺陷
查看标准作业卡
离线缓存常用规程
```

### 14.5 智能培训和考核

基于真实故障案例构建培训系统：

```text
自动生成安规题库
模拟故障排查场景
评估员工处理流程
生成个性化学习建议
沉淀专家经验
```

---

## 16. 简历项目描述版本

### 项目名称

基于大模型的电力智能运维 Multi-Agent 系统

### 项目时间

2025.09 - 2025.12

### 项目介绍

针对电力巡检、监控告警处理、设备故障排障和现场知识查询中存在的知识分散、跨系统查询复杂、故障定位依赖经验等问题，设计并实现了一套基于大模型的电力智能运维 Multi-Agent 系统。系统提供对话式智能问答和告警驱动自动诊断双入口，支持运维人员通过自然语言完成安规查询、设备状态查询、日志分析、历史工单检索和故障排查，也支持接收监控平台推送的结构化告警事件，自动完成告警解析、知识库检索、排查计划生成、工具调用、结果分析和运维建议生成，实现从“用户提问 / 告警触发”到“智能诊断建议输出”的闭环。

### 技术架构

```text
SpringBoot + SpringAI-Alibaba + RAG + ReAct + Plan-Execute-Replan + Multi-Agent + MCP + SSE
```

### 职责描述

1. 基于 SpringAI-Alibaba 设计并实现多 Agent 编排架构，拆分 Router Agent、Chat Agent、Knowledge Agent、Alarm Agent、Planner Agent、Tool Agent、Log Analysis Agent、Ticket Agent 和 Diagnosis Agent，实现电力运维场景下的智能问答、告警诊断和工具调用协同处理。

2. 负责对话式智能问答助手开发，支持运维人员通过自然语言查询电力安规、设备状态、巡检标准、历史缺陷和故障处理流程，并通过多轮上下文记忆实现围绕同一设备或同一告警的连续追问。

3. 基于 RAG 构建电力运维知识库，接入电力安规、设备手册、巡检标准、故障处理规程和历史故障案例等资料，为现场作业指导、安规问答和故障诊断提供知识增强能力，降低大模型幻觉风险。

4. 设计并实现 RAG 文档上传与知识库构建服务，支持原始文档上传、文档解析、文本清洗、分段切片、Embedding 向量化、向量数据库写入和索引构建，为 Knowledge Agent、Planner Agent 和 Diagnosis Agent 提供知识检索能力。

5. 基于 ReAct 模式实现推理与工具调用能力，使 Agent 能够根据用户问题或告警内容动态选择监控查询、日志查询、工单查询、安规查询等工具，完成设备状态查询、异常日志分析和历史缺陷匹配。

6. 基于 Plan-Execute-Replan 模式实现复杂故障诊断流程，构建“告警解析 → 知识库检索 → 排查计划生成 → 工具调用查询 → 中间结果分析 → 动态重规划 → 诊断建议生成”的自动化排障闭环。

7. 通过 MCP 打通监控告警系统、设备状态系统、设备日志系统、缺陷工单系统和安规查询系统，使大模型具备调用真实电力运维数据和外部业务系统的能力。

8. 接入 SSE 实现问答结果、排查步骤和诊断报告的流式输出，优化智能问答和复杂故障诊断场景下的用户交互体验，将故障初步诊断响应时间从小时级降低至分钟级。

---

## 17. 面试讲解版本

这个项目是一个面向电力运维场景的 Multi-Agent 智能系统，核心不是单纯做 RAG 问答，而是把智能问答助手和告警自动诊断结合起来。

系统有两个主要入口。第一个是对话入口，运维人员可以像使用智能助手一样直接提问，比如查询安规、查询设备状态、分析异常日志、查看历史工单，或者问某个故障应该怎么排查。第二个是告警入口，监控平台或者 SCADA 系统产生结构化告警事件后，会推送给 Agent 系统，由 Agent 自动触发诊断流程。

在架构上，我们采用多 Agent 设计。Router Agent 负责识别用户意图和任务路由；Chat Agent 负责多轮上下文管理；Knowledge Agent 负责基于 RAG 检索电力安规、设备手册和历史案例；Alarm Agent 负责解析告警事件；Planner Agent 负责生成排查计划；Tool Agent 通过 MCP 调用外部系统；Log Analysis Agent 负责分析设备日志；Ticket Agent 负责查询历史缺陷工单；Diagnosis Agent 负责综合所有信息生成最终诊断建议。

比如用户问“1号主变油温异常，帮我分析一下原因”，系统会先识别这是故障诊断类问题，然后检索主变油温异常相关规程，规划排查步骤，再调用监控系统查询油温和负荷趋势，调用日志系统查询冷却器运行记录，调用工单系统查询历史缺陷，最后综合生成可能原因和处理建议。

如果是监控平台推送告警，例如“1号主变油温86℃，超过80℃阈值，持续12分钟”，系统也可以自动触发同样的诊断流程。通过 Plan-Execute-Replan，Agent 会先生成初始排查计划，再根据工具返回结果动态调整诊断方向。如果发现冷却器风机启动失败，就会重点转向冷却系统故障分析。

所以这个项目的重点是通过 Multi-Agent、RAG、ReAct、MCP 和 Plan-Execute-Replan，把电力运维中的知识查询、工具调用和故障诊断串成一个完整闭环，既能回答现场人员的问题，也能自动处理监控告警。
