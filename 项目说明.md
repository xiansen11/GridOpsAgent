# 电力智能运维 Multi-Agent 系统（GridOpsAgent）

## 一、项目简介

本项目是一个基于 **Spring Boot 3.2 + Spring AI + 阿里云通义千问（DashScope）** 构建的电力智能运维 Multi-Agent 系统，专门面向电力巡检、监控告警处理、设备故障排障和现场知识查询场景。系统采用多智能体协作架构，结合 RAG 检索增强、知识图谱、Hook 生命周期拦截等先进技术，为电力运维人员提供智能化的辅助决策支持。

### 核心特性

- **Multi-Agent 架构**：6 个专业 Agent + 5 个并行子 Agent 协同工作，支持意图路由、知识问答、告警诊断等
- **工具调用**：8 个电力运维工具 + Tool Registry 工具注册中心 + Tool Search 语义检索
- **Hybrid RAG 知识库**：向量检索 + BM25 关键词检索 + Rerank 重排序 + GraphRAG 知识图谱
- **Hook 生命周期拦截**：12 个 Hook 拦截器，覆盖路由/RAG/工具/诊断全生命周期
- **四层记忆**：Session / Task / Domain / User 四级记忆上下文
- **可观测性**：Trace/Span 链路追踪，自动记录 Agent 执行和工具调用
- **Checkpoint 断点恢复**：诊断流程关键节点自动保存检查点，支持断点续传
- **Prompt Registry**：13 个统一 Prompt 模板管理，支持变量替换
- **RBAC + 人工审批**：基于角色的访问控制 + 高风险操作人工审批流程
- **Plugin 插件**：4 个内置插件 + 生命周期回调 + 执行入口
- **Eval 评测**：6 个内置测试用例 + 评测报告 + 分类通过率统计
- **SSE 流式输出**：真正逐 token 流式输出，支持推理过程实时展示
- **文档版本管理**：知识库文档版本递增、历史查看、版本回滚

---

## 二、技术栈

| 类别 | 技术 | 版本 |
|------|------|------|
| 核心框架 | Spring Boot | 3.2.0 |
| AI 框架 | Spring AI + Spring AI Alibaba | 1.1.0 / 1.1.0.0-RC2 |
| 大模型 | 阿里云通义千问 Qwen3-Max | - |
| 向量嵌入 | DashScope text-embedding-v4 | - |
| 向量数据库 | Milvus | 2.3.3 |
| 关系数据库 | MySQL | 8.0+ |
| 缓存 | Redis | - |
| ORM | MyBatis-Plus | 3.5.5 |
| 文档解析 | Apache POI / PDFBox / Jsoup | 5.2.5 / 3.0.1 / 1.17.2 |
| 构建工具 | Maven | 3.8+ |
| JDK | OpenJDK | 17+ |

---

## 三、整体架构

### 3.1 分层架构总览

系统采用**五层架构**设计，从上到下依次为：接入层 → 编排层 → Agent 层 → 增强层 → 基础设施层。

```
┌─────────────────────────────────────────────────────────────────────┐
│                         接入层（Access Layer）                       │
│   Web UI (index.html)  │  REST API (Controllers)  │  SSE 流式输出   │
└──────────────────────────────────┬──────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────┐
│                       编排层（Orchestration Layer）                   │
│  AgentOrchestrationService — 主编排器                                │
│  ├── HookEngine — 生命周期拦截（12 个 Hook）                         │
│  ├── SkillSelector — 业务场景匹配（5 个 Skill）                      │
│  ├── CheckpointService — 断点保存与恢复                              │
│  └── MemoryService — 四层记忆上下文构建                              │
└──────────────────────────────────┬──────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        Agent 层（Agent Layer）                       │
│                                                                      │
│  ┌──────────┐  ┌──────────────┐  ┌────────────┐  ┌──────────────┐  │
│  │RouterAgent│  │KnowledgeAgent│  │ AlarmAgent │  │LogAnalysis   │  │
│  │ 意图路由  │  │ 知识问答     │  │ 告警分析   │  │Agent 日志分析│  │
│  └──────────┘  └──────────────┘  └────────────┘  └──────────────┘  │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────────────────────┐  │
│  │TicketAgent│  │DiagnosisAgent│  │    SubagentExecutor          │  │
│  │ 工单分析  │  │ 综合诊断     │  │  regulation|metrics|log|     │  │
│  └──────────┘  └──────────────┘  │  ticket|risk_review 并行执行  │  │
│                                   └──────────────────────────────┘  │
│  共享工具层：8 个电力运维工具（ToolCallbackProvider 统一注册）        │
└──────────────────────────────────┬──────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      增强层（Enhancement Layer）                     │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────┐  ┌───────────┐  │
│  │ Hybrid RAG  │  │  GraphRAG    │  │  Rerank   │  │  Prompt   │  │
│  │向量+BM25检索│  │ 知识图谱扩展 │  │  重排序   │  │ Registry  │  │
│  └─────────────┘  └──────────────┘  └───────────┘  └───────────┘  │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────┐  ┌───────────┐  │
│  │    RBAC     │  │  Approval    │  │   Eval    │  │  Plugin   │  │
│  │ 权限控制    │  │ 人工审批流程  │  │  评测框架 │  │  插件管理  │  │
│  └─────────────┘  └──────────────┘  └───────────┘  └───────────┘  │
└──────────────────────────────────┬──────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    基础设施层（Infrastructure Layer）                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │  Milvus  │  │  MySQL   │  │  Redis   │  │  DashScope API   │   │
│  │向量数据库│  │关系数据库│  │  缓存    │  │  大模型+嵌入模型  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 核心架构特点

1. **编排中心模式**：`AgentOrchestrationService` 作为唯一编排入口，负责意图路由、Agent 调度、Hook 拦截、Skill 匹配、Checkpoint 保存和 Memory 注入，所有请求必须经过编排层统一处理。

2. **Hook 拦截链**：在 Agent 执行的关键节点（路由前/后、RAG 前/后、工具调用前/后、诊断前/后）插入拦截器，实现安全检查、权限校验、数据脱敏、审计日志、人工审批等横切关注点，与业务逻辑完全解耦。

3. **Skill 场景匹配**：通过 `SkillSelector` 根据意图或告警类型自动匹配预定义的业务场景（如"主变油温异常诊断"），为 Agent 注入场景化的 Prompt 模板、推荐工具和诊断工作流。

4. **工具共享机制**：所有 Agent 通过统一的 `ToolCallbackProvider` 获取工具集，每个 Agent 可自主决定调用哪些工具（ReAct 模式），无需编排层硬编码工具调用逻辑。

5. **降级容错**：Milvus 不可用时自动降级为 `InMemoryVectorStore`；意图识别失败时降级为 `GENERAL_CHAT`；子 Agent 执行超时（120s）时标记失败但不阻塞整体流程。

---

## 四、Agent 体系与协作机制

### 4.1 Agent 清单

系统包含 **6 个主 Agent** 和 **5 个子 Agent**，每个 Agent 都是基于 Spring AI 的 `ReactAgent`（ReAct 推理+行动模式）实现，具备自主工具调用能力。

#### 主 Agent

| Agent | 类名 | 职责 | 模型温度 | 最大 Token |
|-------|------|------|----------|------------|
| RouterAgent | [RouterAgent.java](src/main/java/org/example/agent/router/RouterAgent.java) | 意图识别路由，将用户问题分类到 9 种意图 | 0.1（高确定性） | 200 |
| KnowledgeAgent | [KnowledgeAgent.java](src/main/java/org/example/agent/knowledge/KnowledgeAgent.java) | 知识库问答，检索电力规程和设备手册 | 0.3 | 2000 |
| AlarmAgent | [AlarmAgent.java](src/main/java/org/example/agent/alarm/AlarmAgent.java) | 告警分析，判断风险等级和紧急性 | 0.3 | 2000 |
| LogAnalysisAgent | [LogAnalysisAgent.java](src/main/java/org/example/agent/log/LogAnalysisAgent.java) | 日志分析，建立异常事件时间线 | 0.3 | 2000 |
| TicketAgent | [TicketAgent.java](src/main/java/org/example/agent/ticket/TicketAgent.java) | 工单分析，查询历史缺陷和关联性 | 0.3 | 2000 |
| DiagnosisAgent | [DiagnosisAgent.java](src/main/java/org/example/agent/diagnosis/DiagnosisAgent.java) | 综合诊断，生成结构化诊断报告 | 0.3 | 3000 |

#### 子 Agent（由 SubagentExecutor 并行调度）

| 子 Agent | 职责 | 触发场景 |
|----------|------|----------|
| regulation | 安规条款查询 | 故障诊断/告警诊断流程 |
| metrics | 设备实时状态查询 | 故障诊断/告警诊断流程 |
| log | 设备日志分析 | 故障诊断/告警诊断流程 |
| ticket | 历史缺陷工单查询 | 故障诊断/告警诊断流程 |
| risk_review | 诊断建议风险复核 | 诊断报告生成后 |

### 4.2 Agent 组织方式

系统采用**中心化编排 + 并行子 Agent** 的组织模式：

```
                    ┌─────────────────────────┐
                    │ AgentOrchestrationService│ ← 唯一编排中心
                    │     （主编排器）          │
                    └────────┬────────────────┘
                             │
              ┌──────────────┼──────────────┐
              ↓              ↓              ↓
      ┌──────────┐   ┌──────────┐   ┌──────────────┐
      │RouterAgent│   │SkillSelect│   │CheckpointSvc │
      │ 意图路由  │   │ 场景匹配  │   │ 断点保存恢复  │
      └─────┬────┘   └─────┬────┘   └──────────────┘
            │              │
            ↓              ↓
   ┌────────────────────────────────┐
   │       意图分发（switch-case）    │
   ├────────┬───────┬───────┬───────┤
   ↓        ↓       ↓       ↓       ↓
简单查询   日志分析  工单查询  故障/告警诊断  通用对话
(ChatAgent) (LogAgent)(TicketAgent)(DiagnosisAgent) (ChatAgent)
                                    │
                            ┌───────┴───────┐
                            │SubagentExecutor│
                            │  并行执行器     │
                            └───┬───┬───┬───┘
                                ↓   ↓   ↓
                          ┌─────────────────┐
                          │regulation|metrics│
                          │log|ticket 并行   │
                          └────────┬────────┘
                                   ↓ 结果聚合
                          ┌─────────────────┐
                          │ DiagnosisAgent   │
                          │ 综合诊断         │
                          └────────┬────────┘
                                   ↓
                          ┌─────────────────┐
                          │ risk_review      │
                          │ 风险复核（串行）  │
                          └─────────────────┘
```

**关键设计决策**：

- **RouterAgent 独立前置**：意图路由使用极低温度（0.1）确保分类确定性，与其他 Agent 的温度（0.3）分离，避免创意性影响分类准确性。
- **简单查询统一处理**：SAFETY_QA、DEVICE_STATUS、ALARM_QUERY、DEVICE_PROFILE 等简单查询意图统一由 `handleSimpleQuery` 方法处理，动态构建 ChatAgent，注入 Skill Prompt + Memory + History 上下文。
- **诊断流程分阶段**：故障诊断采用"并行收集 → 串行综合 → 串行复核"的三阶段模式，先并行收集多维证据，再综合诊断，最后独立复核。

### 4.3 Agent 通信机制

Agent 之间**不直接通信**，而是通过 `AgentOrchestrationService` 编排层进行间接协作，通信方式如下：

| 通信方式 | 说明 | 示例 |
|----------|------|------|
| **意图传递** | RouterAgent 输出意图字符串，编排层根据意图分发到对应 Agent | `{"intent": "FAULT_DIAGNOSIS"}` → 触发诊断流程 |
| **上下文注入** | 编排层将 Skill Prompt、Memory 上下文、对话历史注入到 Agent 的 SystemPrompt 中 | Skill 模板 + Memory 上下文 → ChatAgent 的 systemPrompt |
| **结果聚合** | SubagentExecutor 收集各子 Agent 的文本结果，拼接为结构化上下文传递给 DiagnosisAgent | `regulation 结果 + metrics 结果 + ... → DiagnosisAgent 输入` |
| **Checkpoint 传递** | 编排层在每个关键节点将状态保存到 CheckpointService，恢复时从检查点读取状态继续执行 | `ROUTED → SKILL_SELECTED → RAG_DONE → ...` |
| **Hook 拦截传递** | HookContext 在 Hook 链中传递，Hook 可修改 input/output 实现跨 Agent 的数据加工 | PreRagHook 改写 Query → 传递给 RAG 检索 |

**通信数据流示意**：

```
RouterAgent.route(question)
    → 返回: {"intent": "FAULT_DIAGNOSIS", "confidence": 0.95}
        ↓ 编排层读取 intent
SkillSelector.selectByIntent("FAULT_DIAGNOSIS")
    → 返回: Skill{promptTemplate: "主变油温异常诊断流程..."}
        ↓ 编排层注入 Skill Prompt
SubagentExecutor.executeParallel([regulation, metrics, log, ticket])
    → 返回: [SubagentTask{result: "安规条款..."}, SubagentTask{result: "设备状态..."}, ...]
        ↓ 编排层聚合结果
DiagnosisAgent.call(question + aggregatedContext + skillPrompt)
    → 返回: "## 1.告警摘要\n## 2.初步判断\n..."
        ↓ 编排层传递诊断结果
SubagentExecutor.executeParallel([risk_review])
    → 返回: "风险复核：诊断建议安全性评估..."
        ↓ 编排层追加复核结果
最终输出 = 诊断结果 + 风险复核结果
```

### 4.4 Agent 协作模式

系统包含三种协作模式：

#### 模式一：串行路由模式（简单查询）

```
用户提问 → Hook(PreRoute) → RouterAgent → Hook(PostRoute) →
    → Skill匹配 → Memory注入 → ChatAgent(带工具) → Hook(PostDiagnosis) → 返回结果
```

适用于：安规问答、设备状态查询、告警查询、设备台账查询等简单场景。单个 Agent + 工具调用即可完成。

#### 模式二：并行收集 + 串行综合模式（故障诊断）

```
用户提问 → Hook(PreRoute) → RouterAgent → Hook(PostRoute) →
    → Skill匹配 → Checkpoint(ROUTED) →
    → SubagentExecutor 并行执行 [regulation, metrics, log, ticket] →
    → Checkpoint(SUBAGENTS_DONE) → 结果聚合 →
    → Hook(PreDiagnosis) → DiagnosisAgent 综合诊断 →
    → Checkpoint(DIAGNOSIS_GENERATED) →
    → SubagentExecutor 串行执行 [risk_review] →
    → Hook(PostDiagnosis) → 返回结果
```

适用于：故障诊断、告警诊断等复杂场景。多个子 Agent 并行收集不同维度的证据，再由 DiagnosisAgent 综合分析。

#### 模式三：SSE 流式模式（实时对话）

```
用户提问 → RagService.queryStream() →
    → HybridSearchService 混合检索 → RerankService 重排序 →
    → KnowledgeGraphService 知识图谱扩展 →
    → DashScope SDK 流式调用 → SSE 事件推送
        → onSearchResults → onReasoningChunk → onContentChunk → onComplete
```

适用于：需要实时流式输出的对话场景。绕过 Agent 编排层，直接走 RAG 检索 + 大模型流式生成。

---

## 五、主要业务链路

### 5.1 对话问答链路

**触发条件**：用户通过 Web UI 或 API 发送问题

**完整链路**：

```
用户输入问题
    ↓
[1] ChatController.chat() 接收请求
    ↓
[2] AgentOrchestrationService.handleChat() 编排处理
    ↓
[3] HookEngine.executeHooks("PRE_ROUTE")
    → PreRouteHook: 输入清洗、XSS过滤、长度限制
    → SafetyCheckHook: 安全关键词检查
    ↓
[4] RouterAgent.route(question) 意图识别
    → 返回意图: SAFETY_QA / DEVICE_STATUS / ALARM_QUERY / ...
    ↓
[5] HookEngine.executeHooks("POST_ROUTE")
    → PostRouteHook: 路由结果合理性校验
    ↓
[6] CheckpointService.saveCheckpoint("ROUTED")
    ↓
[7] SkillSelector.selectByIntent(intent) 场景匹配
    → 匹配到 Skill 则注入 promptTemplate
    ↓
[8] CheckpointService.saveCheckpoint("SKILL_SELECTED")
    ↓
[9] MemoryService.buildContextForAgent() 构建记忆上下文
    → Session记忆 + Task记忆 + Domain记忆 + User记忆
    ↓
[10] 根据意图分发：
    ├── SAFETY_QA / DEVICE_STATUS / ALARM_QUERY / DEVICE_PROFILE
    │   → handleSimpleQuery(): 构建 ChatAgent(注入Skill+Memory+History+工具) → agent.call()
    ├── LOG_ANALYSIS → LogAnalysisAgent.create() → agent.call()
    ├── TICKET_QUERY → TicketAgent.create() → agent.call()
    ├── FAULT_DIAGNOSIS / ALARM_DIAGNOSIS → handleDiagnosis()（见5.2）
    └── GENERAL_CHAT → handleSimpleQuery()（无Skill注入）
    ↓
[11] HookEngine.executeHooks("POST_DIAGNOSIS")
    → PostDiagnosisHook: 风险等级检查
    → HumanApprovalHook: 高风险操作创建审批
    → AuditHook: 审计日志记录
    ↓
[12] CheckpointService.saveCheckpoint("COMPLETED")
    ↓
[13] MemoryService.saveToSession() 保存对话记忆
    ↓
返回结果给用户
```

### 5.2 故障诊断链路

**触发条件**：RouterAgent 识别意图为 `FAULT_DIAGNOSIS` 或 `ALARM_DIAGNOSIS`

**完整链路**：

```
意图识别为 FAULT_DIAGNOSIS / ALARM_DIAGNOSIS
    ↓
[1] CheckpointService.saveCheckpoint("SUBAGENTS_START")
    ↓
[2] SubagentExecutor.executeParallel() 并行启动 4 个子 Agent
    ├── regulation 子Agent: 查询安规条款 → searchSafetyRules 工具
    ├── metrics 子Agent: 查询设备状态 → getDeviceStatus 工具
    ├── log 子Agent: 分析设备日志 → getDeviceLogs 工具
    └── ticket 子Agent: 查询缺陷工单 → getDefectTickets 工具
    （每个子Agent独立创建 ReactAgent，共享工具集，120秒超时）
    ↓
[3] CheckpointService.saveCheckpoint("SUBAGENTS_DONE")
    ↓
[4] 结果聚合：将 4 个子Agent的文本结果拼接为结构化上下文
    → "## regulation 分析结果\n...\n## metrics 分析结果\n..."
    ↓
[5] HookEngine.executeHooks("PRE_DIAGNOSIS")
    → PreDiagnosisHook: 证据充足性检查
    ↓
[6] DiagnosisAgent.call(question + aggregatedContext + skillPrompt)
    → 综合分析所有证据，生成结构化诊断报告（8项内容）
    ↓
[7] CheckpointService.saveCheckpoint("DIAGNOSIS_GENERATED")
    ↓
[8] SubagentExecutor.executeParallel() 启动风险复核子 Agent
    → risk_review 子Agent: 审核诊断建议的安全性和可行性
    ↓
[9] 返回: 诊断报告 + 风险复核结果
```

### 5.3 告警接入与诊断链路

**触发条件**：外部告警系统推送告警事件，或用户通过 Web UI 提交告警

**完整链路**：

```
告警事件接入
    ↓
[1] AlarmController.receiveAlarm() 接收告警
    → 创建 AlarmTask 记录（status=RECEIVED）
    → 返回 taskId
    ↓
[2] AlarmController.diagnoseAlarm() 启动诊断（SSE流式）
    → 查询 AlarmTask 获取告警详情
    → 更新 status=DIAGNOSING
    ↓
[3] AiOpsService.createAlarmDiagnosisAgent() 创建诊断Agent
    → 注入 DIAGNOSIS_PROMPT（8项结构化报告模板）
    → 注入全部 8 个工具
    ↓
[4] ReactAgent.call(diagnosisInput) 执行诊断
    → Agent 自主调用工具：getDeviceStatus → getAlarmHistory → getDeviceLogs → ...
    → 生成结构化诊断报告
    ↓
[5] 更新 AlarmTask（status=COMPLETED, diagnosisResult=诊断结果）
    ↓
[6] SSE 推送诊断结果给前端
```

### 5.4 知识库 RAG 检索链路

**触发条件**：用户通过 SSE 流式对话接口提问

**完整链路**：

```
用户提问（SSE流式）
    ↓
[1] ChatController.chatStream() 接收请求
    ↓
[2] RagService.queryStream() RAG检索+流式生成
    ↓
[3] HybridSearchService.hybridSearch() 混合检索
    ├── VectorSearchService 向量检索（Milvus / InMemoryVectorStore）
    │   → query → embedding → Milvus search → Top-K 向量相似结果
    └── keywordSearch BM25关键词检索
        → 分词 → TF-IDF 计算 → Top-K 关键词匹配结果
    ↓
[4] RRF（Reciprocal Rank Fusion）合并
    → 向量结果和关键词结果按排名加权合并
    → score = VECTOR_WEIGHT/(rank+60) + KEYWORD_WEIGHT/(rank+60)
    ↓
[5] RerankService.rerank() 重排序
    → 对合并结果进行二次排序，提升 Top-K 准确率
    ↓
[6] KnowledgeGraphService.buildGraphContext() 知识图谱扩展
    → 从问题中提取实体（变压器、油温高等）
    → 图谱遍历：告警 → 原因 → 排查步骤 → 安规条款
    → 返回扩展上下文
    ↓
[7] 构建 Prompt = 参考资料上下文 + 图谱上下文 + 用户问题
    ↓
[8] DashScope SDK streamCall() 流式调用大模型
    → 逐 token 生成回答
    ↓
[9] SSE 事件推送
    → onSearchResults: 推送检索结果数量
    → onContentChunk: 逐块推送生成内容
    → onComplete: 推送完成信号
```

### 5.5 知识库文档上传链路

**触发条件**：用户上传文档到知识库

**完整链路**：

```
用户上传文档（PDF/Word/Excel/HTML/TXT）
    ↓
[1] KnowledgeController.uploadDocument() 接收文件
    ↓
[2] KnowledgeBaseService.uploadDocument() 处理上传
    ├── 检查同名文档 → 版本递增（旧版本 enabled=0）
    ├── 保存文件到 ./uploads 目录
    └── 创建 KnowledgeDocument 记录（status=UPLOADED, version=N）
    ↓
[3] DocumentParser 解析文档
    ├── PdfDocumentParser: PDFBox 解析 PDF
    ├── WordDocumentParser: Apache POI 解析 Word
    ├── ExcelDocumentParser: Apache POI 解析 Excel
    ├── HtmlDocumentParser: Jsoup 解析 HTML
    └── TextDocumentParser: 纯文本直接读取
    ↓
[4] DocumentChunkService 分块
    → 按 max-size=800 字符分块，overlap=100 字符重叠
    → 创建 KnowledgeChunk 记录
    ↓
[5] VectorEmbeddingService 向量化
    → DashScope text-embedding-v4 模型生成向量
    ↓
[6] VectorIndexService 索引写入
    → Milvus insert / InMemoryVectorStore put
    → 更新 KnowledgeDocument（chunkCount, embeddingCount, status=INDEXED）
```

---

## 六、项目结构

```
GridOpsAgent/
├── src/main/java/org/example/
│   ├── Main.java                 # Spring Boot 启动类
│   ├── agent/                    # Agent 模块
│   │   ├── alarm/                # 告警分析 Agent
│   │   │   └── AlarmAgent.java
│   │   ├── diagnosis/            # 故障诊断 Agent（集成 Subagent 并行）
│   │   │   └── DiagnosisAgent.java
│   │   ├── knowledge/            # 知识问答 Agent
│   │   │   └── KnowledgeAgent.java
│   │   ├── log/                  # 日志分析 Agent
│   │   │   └── LogAnalysisAgent.java
│   │   ├── router/               # 意图路由 Agent
│   │   │   └── RouterAgent.java
│   │   ├── skill/                # Skill 注册与选择
│   │   │   ├── model/
│   │   │   │   └── Skill.java
│   │   │   └── service/
│   │   │       ├── SkillRegistry.java
│   │   │       └── SkillSelector.java
│   │   ├── subagent/             # 子 Agent 并行执行（5 个子 Agent）
│   │   │   ├── SubagentExecutor.java
│   │   │   └── SubagentTask.java
│   │   ├── ticket/               # 工单分析 Agent
│   │   │   └── TicketAgent.java
│   │   └── tool/                 # 工具定义
│   │       ├── DateTimeTools.java
│   │       ├── InternalDocsTools.java
│   │       └── power/            # 电力运维工具集（8 个工具）
│   │           ├── PowerAlarmHistoryTools.java
│   │           ├── PowerDefectTicketTools.java
│   │           ├── PowerDeviceLogsTools.java
│   │           ├── PowerDeviceProfileTools.java
│   │           ├── PowerDeviceStatusTools.java
│   │           └── PowerSafetyRulesTools.java
│   ├── checkpoint/               # 任务检查点服务（自动保存 + 断点恢复）
│   │   └── CheckpointService.java
│   ├── client/                   # Milvus 客户端工厂
│   │   └── MilvusClientFactory.java
│   ├── config/                   # 配置类
│   │   ├── DashScopeConfig.java
│   │   ├── DocumentChunkConfig.java
│   │   ├── FileUploadConfig.java
│   │   ├── HookConfig.java       # Hook 自动注册配置
│   │   ├── MilvusConfig.java     # Milvus 可选配置
│   │   ├── MilvusProperties.java
│   │   ├── MyBatisPlusConfig.java
│   │   ├── ToolConfig.java       # 工具注册 + ToolRegistry 初始化
│   │   ├── WebConfig.java
│   │   └── WebMvcConfig.java
│   ├── constant/
│   │   └── MilvusConstants.java
│   ├── controller/               # REST API 控制器
│   │   ├── AlarmController.java       # 告警诊断 + 断点恢复 API
│   │   ├── ApprovalController.java    # 审批管理 API
│   │   ├── ChatController.java        # 对话 + SSE 流式 API
│   │   ├── EvalController.java        # 评测 API
│   │   ├── KnowledgeController.java   # 知识库 + 版本管理 API
│   │   ├── MilvusCheckController.java # Milvus 健康检查 API
│   │   ├── ObservabilityController.java # 可观测性 API
│   │   ├── SkillController.java       # Skill 管理 API
│   │   └── ToolSearchController.java  # 工具检索 API
│   ├── dto/                      # 数据传输对象
│   │   ├── AIOpsRequest.java
│   │   ├── DocumentChunk.java
│   │   └── FileUploadRes.java
│   ├── entity/                   # 数据实体
│   │   ├── AgentExecutionLog.java
│   │   ├── AlarmTask.java
│   │   ├── ChatMessage.java
│   │   ├── ChatSession.java
│   │   ├── CheckpointRecord.java
│   │   ├── KnowledgeChunk.java
│   │   ├── KnowledgeDocument.java
│   │   ├── KnowledgeProcessTask.java
│   │   ├── ToolCallLog.java
│   │   └── ToolInfo.java
│   ├── eval/                     # 评估框架
│   │   ├── EvalCase.java
│   │   ├── EvalDataset.java
│   │   └── EvalService.java
│   ├── hook/                     # Hook 拦截器
│   │   ├── AgentHook.java
│   │   ├── HookContext.java
│   │   ├── HookEngine.java
│   │   ├── HookResult.java
│   │   └── impl/                 # 12 个 Hook 实现
│   │       ├── AuditHook.java
│   │       ├── DataMaskingHook.java
│   │       ├── HumanApprovalHook.java
│   │       ├── PostDiagnosisHook.java
│   │       ├── PostRagHook.java
│   │       ├── PostRouteHook.java
│   │       ├── PostToolUseHook.java
│   │       ├── PreDiagnosisHook.java
│   │       ├── PreRagHook.java
│   │       ├── PreRouteHook.java
│   │       ├── PreToolUseHook.java
│   │       └── SafetyCheckHook.java
│   ├── mapper/                   # MyBatis Mapper
│   │   ├── AgentExecutionLogMapper.java
│   │   ├── AlarmTaskMapper.java
│   │   ├── ChatMessageMapper.java
│   │   ├── ChatSessionMapper.java
│   │   ├── CheckpointRecordMapper.java
│   │   ├── KnowledgeChunkMapper.java
│   │   ├── KnowledgeDocumentMapper.java
│   │   ├── KnowledgeProcessTaskMapper.java
│   │   ├── ToolCallLogMapper.java
│   │   └── ToolInfoMapper.java
│   ├── memory/                   # 四层记忆服务
│   │   └── MemoryService.java
│   ├── observability/            # 可观测性服务
│   │   └── ObservabilityService.java
│   ├── plugin/                   # 插件管理
│   │   ├── PluginInfo.java
│   │   ├── PluginLifecycle.java
│   │   └── PluginManager.java
│   ├── prompt/                   # Prompt Registry
│   │   └── PromptRegistry.java
│   ├── rag/                      # RAG 增强模块
│   │   ├── HybridSearchService.java    # 混合检索（向量 + BM25 + RRF）
│   │   ├── KnowledgeGraphService.java  # GraphRAG 知识图谱
│   │   └── RerankService.java          # 重排序
│   ├── security/                 # 安全服务
│   │   ├── ApprovalService.java
│   │   └── RbacService.java
│   ├── service/                  # 业务服务
│   │   ├── AgentOrchestrationService.java  # 主编排
│   │   ├── AiOpsService.java               # Agent 创建工厂
│   │   ├── ChatService.java                # 对话服务
│   │   ├── DocumentChunkService.java       # 文档分块服务
│   │   ├── InMemoryVectorStore.java        # 内存向量存储（Milvus 不可用时降级）
│   │   ├── KnowledgeBaseService.java       # 知识库（版本管理）
│   │   ├── RagService.java                 # RAG 服务（集成 Hybrid/Rerank/GraphRAG）
│   │   ├── VectorEmbeddingService.java     # 向量嵌入服务
│   │   ├── VectorIndexService.java         # 向量索引服务
│   │   ├── VectorSearchService.java        # 向量检索服务
│   │   └── parser/                         # 文档解析器
│   │       ├── DocumentParser.java
│   │       ├── ExcelDocumentParser.java
│   │       ├── HtmlDocumentParser.java
│   │       ├── ParsedDocument.java
│   │       ├── PdfDocumentParser.java
│   │       ├── TextDocumentParser.java
│   │       └── WordDocumentParser.java
│   └── tool/                     # 工具管理
│       ├── DropCollection.java
│       ├── ToolRegistryService.java   # 工具注册中心
│       └── ToolSearchService.java     # 工具语义检索
├── src/main/resources/
│   ├── application.yml           # 主配置文件
│   ├── schema.sql                # 数据库初始化脚本
│   └── static/
│       └── index.html            # 前端页面
├── aiops-docs/                   # 知识库文档
│   ├── 主变压器运行维护规程.md
│   ├── 开关柜局放诊断手册.md
│   ├── 电力安规_变电部分.md
│   └── 配网线路故障抢修指导书.md
├── docker-compose.yml            # Docker 编排（Milvus + etcd + MinIO）
├── Makefile                      # 自动化脚本
└── pom.xml                       # Maven 配置
```

---

## 七、功能模块详解

### 7.1 Multi-Agent 架构

系统采用多智能体协作架构，由 RouterAgent 进行意图识别后路由到对应的专业 Agent 处理：

| Agent | 类文件 | 职责 |
|-------|--------|------|
| RouterAgent | [RouterAgent.java](src/main/java/org/example/agent/router/RouterAgent.java) | 意图识别，路由到对应专业 Agent |
| KnowledgeAgent | [KnowledgeAgent.java](src/main/java/org/example/agent/knowledge/KnowledgeAgent.java) | 知识库问答，检索电力规程、设备手册 |
| AlarmAgent | [AlarmAgent.java](src/main/java/org/example/agent/alarm/AlarmAgent.java) | 告警分析，判断风险等级 |
| LogAnalysisAgent | [LogAnalysisAgent.java](src/main/java/org/example/agent/log/LogAnalysisAgent.java) | 日志分析，建立事件时间线 |
| TicketAgent | [TicketAgent.java](src/main/java/org/example/agent/ticket/TicketAgent.java) | 工单分析，查询历史缺陷 |
| DiagnosisAgent | [DiagnosisAgent.java](src/main/java/org/example/agent/diagnosis/DiagnosisAgent.java) | 综合诊断，集成 Subagent 并行分析 + 风险复核 |

**Subagent 并行执行**：诊断流程自动启动 5 个子 Agent 并行分析：

| 子 Agent | 职责 |
|----------|------|
| regulation | 安规条款查询 |
| metrics | 设备实时状态查询 |
| log | 设备日志分析 |
| ticket | 历史缺陷工单查询 |
| risk_review | 诊断建议风险复核 |

### 7.2 工具集 + Tool Registry

系统内置 8 个电力运维工具，通过 Tool Registry 统一注册管理：

| 工具名 | 类文件 | 功能 | 风险等级 |
|--------|--------|------|----------|
| getCurrentDateTime | [DateTimeTools.java](src/main/java/org/example/agent/tool/DateTimeTools.java) | 获取当前时间 | LOW |
| getDeviceStatus | [PowerDeviceStatusTools.java](src/main/java/org/example/agent/tool/power/PowerDeviceStatusTools.java) | 查询设备实时状态 | LOW |
| getAlarmHistory | [PowerAlarmHistoryTools.java](src/main/java/org/example/agent/tool/power/PowerAlarmHistoryTools.java) | 查询历史告警记录 | LOW |
| getDeviceLogs | [PowerDeviceLogsTools.java](src/main/java/org/example/agent/tool/power/PowerDeviceLogsTools.java) | 查询设备运行日志 | LOW |
| getDefectTickets | [PowerDefectTicketTools.java](src/main/java/org/example/agent/tool/power/PowerDefectTicketTools.java) | 查询缺陷工单 | LOW |
| searchSafetyRules | [PowerSafetyRulesTools.java](src/main/java/org/example/agent/tool/power/PowerSafetyRulesTools.java) | 检索安规条款 | LOW |
| getDeviceProfile | [PowerDeviceProfileTools.java](src/main/java/org/example/agent/tool/power/PowerDeviceProfileTools.java) | 查询设备台账信息 | LOW |
| queryInternalDocs | [InternalDocsTools.java](src/main/java/org/example/agent/tool/InternalDocsTools.java) | 知识库检索 | LOW |

**Tool Search**：支持按意图、关键词、标签检索工具，并基于 RBAC 权限过滤。

### 7.3 Hybrid RAG + Rerank + GraphRAG

- **混合检索**（[HybridSearchService.java](src/main/java/org/example/rag/HybridSearchService.java)）：向量检索（Milvus）+ BM25 关键词检索，使用 RRF（Reciprocal Rank Fusion）合并
- **重排序**（[RerankService.java](src/main/java/org/example/rag/RerankService.java)）：对检索结果进行二次排序，提升 Top-K 准确率
- **GraphRAG 知识图谱**（[KnowledgeGraphService.java](src/main/java/org/example/rag/KnowledgeGraphService.java)）：21 个节点 + 17 条边，覆盖变压器/开关柜/线路/GIS 设备的告警→原因→排查→安规路径
- **文档解析**：支持 PDF、Word、Excel、HTML、TXT 格式
- **文档版本管理**：同名文档上传自动版本递增，支持版本列表查看和版本回滚

### 7.4 Hook 生命周期拦截

| Hook Point | Hook 类 | 功能 |
|------------|---------|------|
| PRE_ROUTE | [PreRouteHook.java](src/main/java/org/example/hook/impl/PreRouteHook.java) | 输入清洗、XSS 过滤、长度限制 |
| POST_ROUTE | [PostRouteHook.java](src/main/java/org/example/hook/impl/PostRouteHook.java) | 路由结果合理性校验 |
| PRE_RAG | [PreRagHook.java](src/main/java/org/example/hook/impl/PreRagHook.java) | Query 改写（补全设备类型前缀） |
| POST_RAG | [PostRagHook.java](src/main/java/org/example/hook/impl/PostRagHook.java) | 召回质量检查 |
| PRE_TOOL_USE | [PreToolUseHook.java](src/main/java/org/example/hook/impl/PreToolUseHook.java) | 权限校验、高风险工具标记 |
| POST_TOOL_USE | [PostToolUseHook.java](src/main/java/org/example/hook/impl/PostToolUseHook.java) | 结果脱敏、审计日志记录 |
| PRE_DIAGNOSIS | [PreDiagnosisHook.java](src/main/java/org/example/hook/impl/PreDiagnosisHook.java) | 证据充足性检查 |
| POST_DIAGNOSIS | [PostDiagnosisHook.java](src/main/java/org/example/hook/impl/PostDiagnosisHook.java) | 风险等级检查、安全提示补全 |
| POST_DIAGNOSIS | [HumanApprovalHook.java](src/main/java/org/example/hook/impl/HumanApprovalHook.java) | 高风险操作创建审批请求 |
| 全生命周期 | [AuditHook.java](src/main/java/org/example/hook/impl/AuditHook.java) | 审计日志 |
| POST_TOOL_USE | [DataMaskingHook.java](src/main/java/org/example/hook/impl/DataMaskingHook.java) | 手机号/密码脱敏 |
| 全生命周期 | [SafetyCheckHook.java](src/main/java/org/example/hook/impl/SafetyCheckHook.java) | 安全关键词检查 |

### 7.5 Checkpoint 断点恢复

诊断流程在以下节点自动保存检查点：

```
RECEIVED → ROUTED → SKILL_SELECTED → RAG_DONE →
SUBAGENTS_DONE → DIAGNOSIS_GENERATED → WAITING_APPROVAL → COMPLETED
```

支持通过 API 从任意检查点恢复执行。

### 7.6 Memory 四层记忆

| 层级 | 作用域 | TTL | 说明 |
|------|--------|-----|------|
| Session | 单次会话 | 2 小时 | 当前会话的对话上下文 |
| Task | 单次任务 | 24 小时 | 单次诊断任务的中间状态 |
| Domain | 领域知识 | 永久 | 电力运维领域常识 |
| User | 用户偏好 | 30 天 | 用户习惯和偏好设置 |

### 7.7 Prompt Registry

统一管理 13 个 Prompt 模板，支持 `{{变量}}` 替换：

| Key | 用途 |
|-----|------|
| router | 意图路由 |
| chat | 通用对话 |
| knowledge | 知识问答 |
| alarm | 告警分析 |
| diagnosis | 故障诊断 |
| log_analysis | 日志分析 |
| ticket | 工单分析 |
| rag | RAG 检索增强 |
| subagent_regulation | 安规查询子 Agent |
| subagent_metrics | 设备状态子 Agent |
| subagent_log | 日志分析子 Agent |
| subagent_ticket | 工单分析子 Agent |
| subagent_risk_review | 风险复核子 Agent |

### 7.8 RBAC + 人工审批

| 角色 | 权限 |
|------|------|
| admin | 全部权限（含系统配置、审批、用户管理） |
| operator | 对话、诊断、知识检索、告警处理、工单创建 |
| viewer | 对话、知识检索、查看可观测性 |

高风险操作（停电、降负荷、紧急派单）自动触发人工审批流程。

### 7.9 Observability 可观测性

- **Trace/Span**：自动记录每个 Agent 调用的 Trace 和 Span
- **工具调用日志**：记录每次工具调用的输入、输出、耗时
- **Trace 可视化**：通过 API 查询完整的调用链

### 7.10 Plugin 插件

| 插件 | 类别 | 功能 |
|------|------|------|
| power-monitoring | monitoring | 设备状态监控、告警历史查询 |
| power-safety | safety | 安规查询、安全措施提示 |
| power-diagnosis | diagnosis | 多 Agent 协作故障诊断 |
| power-knowledge | knowledge | RAG 知识库检索、文档上传 |

支持 `PluginLifecycle` 接口：`onInstall` / `onUninstall` / `onEnable` / `onDisable` / `execute`

### 7.11 Eval 评测

- 6 个内置测试用例，覆盖安规问答、设备状态、告警诊断等场景
- 评测指标：响应时间、关键词匹配率、安全提示覆盖率
- 评测报告：总通过率、分类通过率、平均响应时间

---

## 八、快速开始

### 8.1 环境要求

| 依赖 | 版本要求 | 说明 |
|------|----------|------|
| JDK | 17+ | 推荐 Eclipse Adoptium Temurin 17 |
| Maven | 3.8+ | 用于项目构建 |
| Docker Desktop | 最新版 | 用于运行 Milvus 向量数据库 |
| MySQL | 8.0+ | 关系数据库 |
| Redis | 6.0+ | 缓存（可选，未配置时使用内存缓存） |

### 8.2 启动基础设施

#### 8.2.1 启动 Milvus

```bash
# 确保 Docker Desktop 已启动，然后执行
docker compose up -d

# 查看容器状态（应看到 milvus-standalone、milvus-etcd、milvus-minio 三个容器）
docker ps --filter "name=milvus"
```

Milvus 相关端口：
- `19530`：Milvus gRPC 端口
- `9091`：Milvus 健康检查端口
- `9000`：MinIO API 端口
- `9001`：MinIO 控制台端口（账号/密码：minioadmin/minioadmin）

> 如果暂时不需要 Milvus，可以在 `application.yml` 中设置 `milvus.enabled: false`，系统会自动使用内存向量存储。

#### 8.2.2 初始化 MySQL

```bash
# 创建数据库
mysql -uroot -proot -e "CREATE DATABASE IF NOT EXISTS power_aiops DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 导入表结构
mysql -uroot -proot power_aiops < src/main/resources/schema.sql
```

schema.sql 会自动创建以下表：
- `chat_session` — 对话会话
- `chat_message` — 对话消息
- `alarm_task` — 告警任务
- `agent_execution_log` — Agent 执行日志
- `tool_call_log` — 工具调用日志
- `knowledge_document` — 知识库文档
- `knowledge_chunk` — 知识库分块
- `knowledge_process_task` — 文档处理任务
- `tool_registry` — 工具注册表
- `skill_definition` — Skill 定义
- `checkpoint_record` — 检查点记录

### 8.3 配置 API Key

编辑 `src/main/resources/application.yml`，配置阿里云 DashScope API Key：

```yaml
spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:sk-xxxxxxxxxxxxxxxx}

dashscope:
  api:
    key: ${DASHSCOPE_API_KEY:sk-xxxxxxxxxxxxxxxx}
```

> 获取 API Key：访问 [阿里云 DashScope 控制台](https://dashscope.console.aliyun.com/) 注册并创建 API Key。

也可以通过环境变量设置（推荐）：

```bash
# Windows PowerShell
$env:DASHSCOPE_API_KEY = "sk-xxxxxxxxxxxxxxxx"

# Linux/Mac
export DASHSCOPE_API_KEY="sk-xxxxxxxxxxxxxxxx"
```

### 8.4 构建项目

**Windows PowerShell：**

```powershell
# 设置 JDK 17 环境（根据实际安装路径调整）
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-17.0.17.10-hotspot"
$env:PATH = "$env:JAVA_HOME\bin;$env:PATH"

# 构建
mvn clean package -DskipTests -q
```

**Linux/Mac：**

```bash
export JAVA_HOME=/path/to/jdk-17
mvn clean package -DskipTests -q
```

### 8.5 启动应用

**方式一：JAR 包启动**

```bash
java -jar target/grid-ops-agent-1.0-SNAPSHOT.jar
```

**方式二：Maven 启动**

```bash
mvn spring-boot:run
```

**方式三：Makefile 一键初始化（Linux/Mac）**

```bash
make init
```

该命令会自动：启动 Docker → 启动 Spring Boot → 等待服务就绪 → 上传知识库文档。

### 8.6 访问系统

启动成功后，打开浏览器访问：

| 入口 | 地址 | 说明 |
|------|------|------|
| Web UI | http://localhost:9900 | 电力智能运维平台前端页面 |
| API 基础路径 | http://localhost:9900/api | REST API 接口 |

---

## 九、API 接口文档

### 9.1 对话接口

#### 普通对话

```http
POST /api/chat
Content-Type: application/json

{
  "question": "查询 TR-110KV-001 主变的当前油温",
  "sessionId": "session-001",
  "userId": "admin"
}
```

响应：
```json
{
  "sessionId": "session-001",
  "answer": "根据查询结果，TR-110KV-001 主变当前油温为 72℃..."
}
```

#### SSE 流式对话

```http
POST /api/chat/stream
Content-Type: application/json

{
  "question": "查询 TR-110KV-001 主变的当前油温",
  "sessionId": "session-001",
  "userId": "admin"
}
```

SSE 事件类型：
- `search_results` — 检索结果通知
- `reasoning` — 推理过程（逐块）
- `message` — 内容块（逐 token）
- `done` — 完成，数据为 `[DONE]`
- `error` — 错误信息

#### 清空会话

```http
POST /api/chat/clear
Content-Type: application/json

{
  "sessionId": "session-001"
}
```

### 9.2 告警诊断接口

#### 接收告警

```http
POST /api/alarm/receive
Content-Type: application/json

{
  "alarmId": "ALM-20240101-001",
  "station": "XX变电站",
  "deviceId": "TR-110KV-001",
  "deviceName": "1号主变压器",
  "deviceType": "变压器",
  "alarmType": "油温高告警",
  "alarmLevel": "重要",
  "currentValue": "85",
  "threshold": "75",
  "duration": "30分钟"
}
```

响应：
```json
{
  "taskId": "TASK-xxxxxxxx",
  "alarmId": "ALM-20240101-001",
  "status": "RECEIVED",
  "message": "告警已接收，请调用 /api/alarm/diagnose 启动诊断"
}
```

#### 启动诊断（SSE 流式）

```http
POST /api/alarm/diagnose
Content-Type: application/json

{
  "taskId": "TASK-xxxxxxxx",
  "alarmDescription": "1号主变油温异常偏高"
}
```

#### 查询诊断状态

```http
GET /api/alarm/diagnose/{taskId}/status
```

#### 断点恢复

```http
POST /api/alarm/resume/{taskId}
```

#### 查询检查点

```http
GET /api/alarm/checkpoint/{taskId}
```

### 9.3 知识库接口

#### 上传文档

```http
POST /api/knowledge/documents/upload
Content-Type: multipart/form-data

file: 文档文件（支持 pdf/docx/xlsx/html/txt/md）
documentType: 文档类型（如 电力安规、设备手册）
description: 文档描述
tags: 标签（逗号分隔）
```

#### 查询文档状态

```http
GET /api/knowledge/documents/{documentId}/status
```

#### 文档列表

```http
GET /api/knowledge/documents?documentType=电力安规&page=1&size=20
```

#### 删除文档

```http
DELETE /api/knowledge/documents/{documentId}
```

#### 检索测试

```http
POST /api/knowledge/search/test
Content-Type: application/json

{
  "query": "变压器油温高的处理方法",
  "topK": 5
}
```

#### 文档版本管理

```http
GET /api/knowledge/documents/{documentName}/versions
POST /api/knowledge/documents/{documentId}/rollback
```

### 9.4 工具检索接口

```http
GET /api/tools/list                                    # 列出所有工具
GET /api/tools/search?keyword=设备                      # 关键词搜索
GET /api/tools/search/intent?intent=FAULT_DIAGNOSIS&userId=admin  # 意图搜索
GET /api/tools/search/tags?tags=查询,监控                # 标签搜索
GET /api/tools/high-risk                               # 高风险工具列表
POST /api/tools/refresh                                # 刷新工具缓存
```

### 9.5 审批管理接口

```http
POST /api/approval/request       # 创建审批请求
POST /api/approval/approve       # 审批通过
POST /api/approval/reject        # 审批拒绝
GET /api/approval/pending        # 待审批列表
```

### 9.6 Skill 管理接口

```http
GET /api/skills                           # 列出所有 Skill
GET /api/skills?category=diagnosis        # 按类别筛选
GET /api/skills/{skillId}                 # 获取 Skill 详情
POST /api/skills/select                   # 选择 Skill
```

### 9.7 评测接口

```http
POST /api/eval/run    # 运行所有评测用例
```

### 9.8 可观测性接口

```http
GET /api/observability/traces/{traceId}   # 查询 Trace 详情
```

### 9.9 Milvus

```http
GET /milvus/health    # Milvus 连接状态检查
```

---

## 十、配置说明

### application.yml 主要配置项

```yaml
server:
  port: 9900                              # 服务端口

spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}       # 阿里云 DashScope API Key
      chat:
        options:
          timeout: 180000                 # 大模型调用超时（3 分钟）
      retry:
        max-attempts: 3                   # 重试次数
        backoff:
          initial-interval: 2000          # 初始重试间隔
          multiplier: 2                   # 重试间隔倍数
          max-interval: 10000             # 最大重试间隔

  datasource:
    url: jdbc:mysql://localhost:3306/power_aiops?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:root}

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

milvus:
  enabled: true                           # 是否启用 Milvus（false 时使用内存向量存储）
  host: localhost
  port: 19530
  timeout: 10000

dashscope:
  api:
    key: ${DASHSCOPE_API_KEY}
  embedding:
    model: text-embedding-v4              # 向量嵌入模型

document:
  chunk:
    max-size: 800                         # 文档分块最大字符数
    overlap: 100                          # 分块重叠字符数

rag:
  top-k: 3                                # RAG 检索 TopK
  model: "qwen3-max"                      # 大模型名称

power:
  mock-enabled: true                      # 是否使用模拟数据（true 时使用内置模拟数据）
  alarm:
    levels: 紧急,重要,一般,提示              # 告警等级
  device-types: 变压器,开关柜,断路器,隔离开关,母线,电缆,互感器  # 设备类型
  document-types: 电力安规,设备手册,巡检标准,故障处理规程,历史故障案例,专家经验文档,厂家说明书,标准作业指导书  # 文档类型

file:
  upload:
    path: ./uploads                       # 文件上传路径
    allowed-extensions: txt,md,pdf,docx,doc,xlsx,xls,html  # 允许的文件扩展名
```

---

## 十一、常见问题

### Q1: 启动时提示 "Failed to connect to Milvus"

**A:** 确保 Milvus 容器已启动：
```bash
docker compose up -d
docker ps --filter "name=milvus"
```

如果暂时不需要 Milvus，可以在 `application.yml` 中设置 `milvus.enabled: false`，系统会自动使用内存向量存储。

### Q2: 启动时提示 MySQL 连接失败

**A:** 确保 MySQL 已启动并创建了数据库：
```bash
mysql -uroot -proot -e "CREATE DATABASE IF NOT EXISTS power_aiops DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
mysql -uroot -proot power_aiops < src/main/resources/schema.sql
```

如果 MySQL 端口或密码不同，请修改 `application.yml` 中的 `spring.datasource` 配置。

### Q3: 启动时提示 "Invalid API Key"

**A:** 检查 `application.yml` 中的 `spring.ai.dashscope.api-key` 和 `dashscope.api.key` 配置是否正确，确保已替换为有效的阿里云 DashScope API Key。

### Q4: 如何添加新的知识文档？

**A:** 有两种方式：
1. 将文档放入 `aiops-docs` 目录，然后调用索引接口
2. 通过 Web API 上传文档

```bash
# 方式一：目录索引
curl -X POST "http://localhost:9900/api/knowledge/index?directoryPath=./aiops-docs"

# 方式二：单文件上传
curl -X POST "http://localhost:9900/api/knowledge/documents/upload" \
  -F "file=@你的文档.pdf" \
  -F "documentType=设备手册"
```

### Q5: 如何切换到真实设备数据？

**A:** 在 `application.yml` 中设置 `power.mock-enabled: false`，并配置真实数据源接口。当前系统默认使用模拟数据。

### Q6: 工具名称重复报错 "Multiple tools with the same name"

**A:** 确保每个工具只在 `ToolConfig.java` 的 `toolCallbackProvider()` 中注册一次，不要在 Agent 中重复注入。

### Q7: JDK 版本不匹配

**A:** 本项目要求 JDK 17+，请确保 `JAVA_HOME` 指向 JDK 17 安装路径：
```powershell
# Windows PowerShell
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-17.0.17.10-hotspot"
$env:PATH = "$env:JAVA_HOME\bin;$env:PATH"
java -version
```

---

## 十二、开发指南

### 12.1 添加新工具

1. 在 `agent/tool/power/` 下创建工具类：

```java
@Component
public class PowerNewTools {

    @Tool(description = "新工具描述")
    public String newFunction(@ToolParam(description = "参数描述") String param) {
        return "result";
    }
}
```

2. 在 `ToolConfig.java` 中注册：

```java
@Autowired
private PowerNewTools powerNewTools;

@Bean
public ToolCallbackProvider toolCallbackProvider() {
    return MethodToolCallbackProvider.builder()
            .toolObjects(powerNewTools)
            .build();
}

@PostConstruct
public void registerToolsToRegistry() {
    toolRegistryService.registerTool("newFunction", "newFunction",
            "新工具描述", null, null,
            List.of("tag1", "tag2"), "QUERY", "LOW", "SYSTEM");
}
```

### 12.2 添加新 Agent

1. 在 `agent/` 下创建 Agent 类：

```java
@Component
public class NewAgent {

    @Autowired
    private ToolCallbackProvider tools;

    public ReactAgent create(DashScopeApi dashScopeApi) {
        DashScopeChatModel chatModel = DashScopeChatModel.builder()
                .dashScopeApi(dashScopeApi)
                .build();

        return ReactAgent.builder()
                .name("new_agent")
                .model(chatModel)
                .systemPrompt("系统提示词")
                .tools(tools.getToolCallbacks())
                .build();
    }
}
```

2. 在 `AgentOrchestrationService` 或 `AiOpsService` 中集成新 Agent。

### 12.3 添加新 Hook

1. 在 `hook/impl/` 下创建 Hook 类：

```java
@Component
public class CustomHook implements AgentHook {
    @Override
    public String getName() { return "custom_hook"; }

    @Override
    public int getOrder() { return 20; }

    @Override
    public HookResult execute(HookContext context) {
        // 自定义逻辑
        return HookResult.proceed();
    }
}
```

2. 在 `HookConfig.java` 中注册到对应 hookPoint：

```java
hookEngine.registerHook("POST_DIAGNOSIS", customHook);
```

### 12.4 添加新 Prompt

在 `PromptRegistry.java` 的 `init()` 方法中添加：

```java
register("custom_prompt", "自定义提示词",
        "你的自定义模板，支持 {{variable}} 变量替换");
```

使用时：

```java
String prompt = promptRegistry.getPrompt("custom_prompt",
    Map.of("variable", "实际值"));
```

### 12.5 添加新 Skill

在 `SkillRegistry.java` 的初始化方法中注册：

```java
registerSkill(Skill.builder()
        .skillId("custom_skill")
        .name("自定义 Skill")
        .description("描述")
        .category("custom")
        .applicableScenarios(List.of("场景1", "场景2"))
        .recommendedTools(List.of("tool1", "tool2"))
        .build());
```

---

## 十三、数据库表结构

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| chat_session | 对话会话 | session_id, user_id |
| chat_message | 对话消息 | session_id, role, content, intent |
| alarm_task | 告警任务 | task_id, alarm_id, device_id, status, diagnosis_result |
| agent_execution_log | Agent 执行日志 | trace_id, agent_name, step_name, duration_ms |
| tool_call_log | 工具调用日志 | trace_id, tool_name, request_param, response_data |
| knowledge_document | 知识库文档 | document_id, document_name, version, status |
| knowledge_chunk | 知识库分块 | chunk_id, document_id, content |
| knowledge_process_task | 文档处理任务 | task_id, document_id, status |
| tool_registry | 工具注册表 | tool_id, tool_name, risk_level, permission_level |
| skill_definition | Skill 定义 | skill_id, name, category |
| checkpoint_record | 检查点记录 | task_id, step_name, agent_state |

---

## 许可证

本项目采用 MIT 许可证。
